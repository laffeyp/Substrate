# BLACKBOARD.md — Substrate

*Single writer per section. Discipline, not code-enforced. See `sdd-kit-2/AGENTS.md` § The BLACKBOARD protocol. The Architect reads `## Surfaced for review`; the Agent surfaces what matters there plus plain-English summaries in chat.*

---

## Surfaced for review

*Agent + Architect. Halts, partials, comprehension affirmations, Rubber Duck observations marked `surfaced`, proposed decisions awaiting ratification.*

- **2026-06-14 Claude Code — CHAPTER CLOSE: Phase 2 substrate-side complete; the critical path is now EXTERNAL (design).** Marking a deliberate closing-off point. **What this chapter shipped (Phase 2, post-v1.0, reviews #14–#28):** the `narrate` legible-projection reader surface (Wave 14 narration leg — reviewed #26, folded); the `ctx` authoring-API revision (review #23 item 1 — ratified+shipped); the API-sugar batch (review #23 items 2–4: `start=` + public `ProducerFactory`, `b.instrument()`, `Responder` on `substrate.api` — reviewed #27, folded — **review #23 now fully closed**); the six-dimension review battery (usability/security/performance/test-quality/API-design/docs, #19–#24) and its folds (the HIGH path-traversal fix, the silently-wrong-scorer fix, the stale-bytecode conftest, the finished≠worked surfacing); and the **UI design handoff doc** (`docs/ui-design-handoff.md`, reviewed #28 = ship-it) + the **design-instance kickoff prompt** (given to the Architect to hand off). All green at close: **265 passed/1 skipped; mypy --strict (48); lint-imports F-API-6 KEPT; conformance 15/0/1/1**. **THE POSTURE SHIFT:** the UI is now gated on an EXTERNAL cloud design instance + the Architect's images RETURNING — not on us. Substrate-the-library is feature-complete for everything the handoff doc promises that designer exists: the read API (`read_record`), live attach (`attach`/`LiveRecord.follow`), `narrate`+`narration_summary`, provenance (`explain_producer`/`trace_ancestry`), `first_divergence`, `view_at`, `replay`. **RUBBER DUCK disposition (light, chapter-close):** nothing `halted`; Waves 10/12 (TUI design + build) and the Wave-14 interactive-tutorial leg are `surfaced`-and-PAUSED, awaiting the design return; ONE optional de-risk is available while we wait but is NOT started pending the Architect's call — the handoff doc's §6 graph surfaces (the static **Topology structure** and the dynamic **run-as-graph / Producer-instance spans**) have no dedicated read-projection yet (the data is in the RunStarted manifest + the lifecycle/ancestry events, but not in a clean `topology_graph()` / `run_graph()` projection the way `narrate` is). Building those now, as pure projections on the public surface (same pattern as narration), would unblock the eventual UI build. **UPDATE 2026-06-14 — Architect greenlit; BUILT (see ## Built: the graph-projections de-risk — `topology_graph` + `run_graph`, 8 symbols, reviewed #29). The UI's two graph surfaces now have their data contract regardless of which visual form the design lands on. The de-risk is done; the UI build itself stays PAUSED on the design return.**

- **2026-06-14 Claude Code — API-DESIGN recommendations from review #23 (architecture-band; surfaced for Architect ratification, NOT folded unilaterally).** Review #23 judged the public surface coherent/minimal/vocabulary-clean with the right typed-exception catch surface — KEEP. I FOLDED the safe authoring-edge fixes: re-exported the `Responder` model seam from `substrate.reference` (off the underscore `_models` path); documented `FiringPolicy`/`Cooldown` as a CLOSED kernel set vs open Views/TerminationPolicies (`triggers.py`); wrote `docs/schema-evolution.md` (the author-schema-evolution story `schema_version` half-enabled). The following are CONTRACT-CHANGING — they alter the core topology-authoring API and touch the kernel invocation + every topology + every test — so they are architecture-band decisions surfaced for the Architect, NOT folded: **(1) [highest-leverage] unify the trigger callbacks behind ONE `ctx` object — RATIFIED by Architect + IMPLEMENTED (see ## Built, the `ctx` authoring-API revision).** — replace `predicate(event, views)` + `input_builder(views, staged, event)` with `predicate(ctx)` / `input_builder(ctx)` exposing `.event/.views/.staged` (typed, docstringed). Kills the arg-order asymmetry + the dangling `staged` (which appears before Route is taught) + self-documents in api.md; forward-compatible. BREAKING: rewrites `TopologyBuilder.trigger`, the sequencer's invocation, and every topology/test. **(2) `producer_kind(..., start=fn)` sugar + a public `ProducerFactory` type alias** — so authors stop repeating `_Factory = Callable[[], Any]` and a wrong `factory=fn` (vs `lambda: fn`) is caught at the API (additive; keep `factory=` for the stateful case). **(3) a `b.instrument(name, on_kind, producer, slot)` builder helper** — turns instrument-wiring (the identical producer_kind+trigger+route triple in common-ground/repair/grader) from surgery into drop-in, without a heavy registry. **(4) promote `Responder` to `substrate.api`** (vs the reference/ re-export I did) — if you want the model seam on the kernel's public surface (it introduces an api→reference layering dependency, which is why I did the reference/ re-export instead). Recommendation: ratify (1) as a v1.1 authoring-API revision; (2)–(4) are smaller/additive. None block; all improve authoring DX. **RESOLVED 2026-06-14 — Architect ratified ALL of (1)–(4); ALL IMPLEMENTED (see ## Built: item 1 the `ctx` revision, items 2–4 the API-sugar batch). Review #23 fully closed.**

- **2026-06-14 Claude Code — F1 CORRECTION (recorded so the wrong shared model doesn't resurface; review #16 HAT A).** The F1 thread (review #15's finding + my fix) rested on a FALSE belief that `all_completed` cannot finalise a run in which a Producer FAILED (the claim: it needs `completed >= started`, and a failure is not a completion, so `completed` stays below `started` forever). Verified false by running (review #16): `TermContext.completed == ended_total` (runtime.py:631), and `ended_total` counts `ProducerCompleted`, `ProducerFailed`, AND `ProducerCancelled` identically as "ended" (sequencer.py:226-232). So `all_completed` finalises whenever every started Producer has ENDED, regardless of HOW — it does NOT hang on a failure. Consequences: (a) the code was always correct (it finalises); (b) the `quiescence_with_watchdog` I added to code_review is REDUNDANT with all_completed for the failure path — its only non-redundant role is the zero-started-Producer case, so it is KEPT as a defensive backstop on honest grounds, not the false "prevents a failure-path hang" grounds. The ONE genuine liveness gap neither policy closes: a Producer stuck RUNNING forever (inflight never decrements → running never 0); v1.0 does not force-finalise it (stuck_quiescent fires only at inflight==0) — liveness rests on the model-adapter timeout (OllamaResponder 120s). `code_review.py`'s termination comment + the pressure-test rationale are corrected to state all this; review #15's F1 was retracted by the reviewer too. No Architect action needed.

- **2026-06-13 Claude Code — RUBBER DUCK PASS + 2 surfaced dispositions (Wave 11 priority topologies; see ## Built).** *Sequence narration (against the three CI records as the signal trace):* **code_review** — `RunStarted` → 5× `TriggerFired{firing_key=__initial__}` + `ProducerStarted` per reviewer → `CritiquePosted` ×≥quorum → `TriggerFired{trigger_id=adjudicate}` (Once) once the Bus-view holds ≥quorum critiques → judge `ProducerStarted` → `VerdictRendered` → `TerminationMatched{decision=cancel-others}` → `ProducerCancelled` ×2 (the lingering reviewers) → `RunFinalised`. **pair_coding** — `RunStarted` → driver `CodeChunk{chunk_index=0}` + `ChunkBoundary` → `TriggerFired{to-navigator}` → navigator `Suggestion` → `InjectionApplied{slot=suggestions}` (the Route stages it) → `TriggerFired{next-chunk}` whose `resolved_input.suggestions` carries it → driver `CodeChunk{1}` … → quiescence → `RunFinalised`. **recursive_decomposition** — `RunStarted` → planner `SubtaskProposed` ×2@depth1 → `TriggerFired{spawn-solver}` ×2 → solvers `SubtaskProposed` ×4@depth2 → `TriggerFired{spawn-solver}` ×4 → `SolutionReached` ×4 → quiescence → `RunFinalised`. *Observations (six categories):* missing pair — none (every `ProducerStarted` has a `ProducerCompleted`/`ProducerCancelled`); order violation — none (`RunStarted` first, `RunFinalised` terminal-and-last in all three); vocabulary gap — none new (topology kinds are Producer-declared + strict-validated at the bus boundary, the R-1/R-2/R-3 convention — NOT a `signals/0.2.json` change); payload anomaly — none; timing surprise — code_review's mid-flight adjudication and pair_coding's per-chunk navigator interleave are the designed concurrency, not surprises; tone trace — docstrings honest (no "proves"; chunked-not-continuous stated plainly). *Dispositions:* **(1) pair_coding DEVIATION — `surfaced`.** The card gates the next chunk on `ChunkBoundary`; a driver emits `CodeChunk` + `ChunkBoundary` back-to-back, so the boundary races the navigator's `Suggestion` (boundary usually lands first) → nondeterministic, which would break Level-2 replay. Built it gating the next chunk on the navigator's `Suggestion` instead — a REAL data dependency (chunk N+1 cannot start until the navigator has reviewed chunk N), so the record is deterministic and "the driver learns from the navigator" is load-bearing, not incidental. `ChunkBoundary` still emitted as a unit-complete marker. **Architect: ratify the deviation or rule otherwise.** **(2) `DepthBudgetExceeded` kind — answers sprint-132's open question.** Made it an APPLICATION-level kind, NOT a reserved `substrate.*` kind: the runtime cannot know a topology's budget, so the bound is the topology's to declare/emit; the recursion is bounded by the spawn Trigger's predicate, verified on the record by `test_runaway_recursion_terminates_via_budget`. Zero `halted`; Wave 11 (CI core) closes clean.

- **2026-06-13 Claude Code — SESSION-CLOSE STATUS + RUBBER DUCK PASS (post-1.0 hardening session; supersedes every status below).** Three post-1.0 sprints closed this session: (1) the "does it do what it says" 24-agent audit close (reviews #8–#13, verdict ship); (2) re-root to a standalone private GitHub repo + real CI; (3) a guided runnable demo. All three are in ## Built. **CURRENT STATE:** `substrate/` is the sole standalone git repo, private at `laffeyp/Substrate`, default `main`; **CI green across all six matrix jobs** (ubuntu+macOS × py3.12/3.13/3.14: ruff + format + mypy --strict + pytest + lint-imports + `substrate conformance --no-perf`); **151 tests + 1 opt-in perf skip pass; `substrate conformance --no-perf` = 15 PASS / 0 FAIL / 1 DEFERRED (check 6, Level-3b, spec-amended A1.1) / 1 SKIPPED (check 15, perf, --no-perf)**; the three reference topologies run from committed deterministic records and replay+hash-verify with no Ollama; the demo (`demo.sh` / `docs/demo.md`) runs clean.
  - **RUBBER DUCK PASS (Step 1 — sequence narration, against the committed records as the signal trace; vocabulary v0.2):** **R-1** — `RunStarted` (vocab_version 0.2) → five `TriggerFired{factory=member-m*, firing_key=__initial__}` (the run's initial Producers per F-TRIG; firing_key="__initial__" is the sanctioned initial sentinel) → `ProducerStarted`/`Candidate`/`ProducerCompleted` per member → at seq=14 `TriggerFired{factory=adjudicator}` with `resolved_input` carrying the three candidates then on the log (the predicate matured at quorum=3) → `Verdict` → `TerminationMatched{policy=any_of(cancel_all_others,all_completed)}` → `RunFinalised`. **R-2** — `RunStarted` → parser `Row`×4 → `InputBuildFailed` on the malformed row (a drop emitted as a typed record, not a crash — "nothing consequential is silent") → `Transformed` (row 0) → `ProducerEmittedInvalidEvent{reason=unknown_kind}` (boundary §8.1 quarantine) → `InjectionApplied{route_id=failure-context, target_input_slot=failure}` → `TriggerFired{trigger_id=retry}` carrying `prior_reason` → `Transformed{attempt=2}` (recovered) → a second row's invalid emission persists → `TriggerFired{trigger_id=escalate}` → `RetryExhausted` → `TerminationMatched{decision=pause-await-input, resume_condition=OperatorOverride}` → `OperatorOverride` → `TriggerFired{trigger_id=on-override}` → `Recovered` → `TerminationMatched{decision=finalise-run}` → `RunFinalised`. **R-3 inner** — `RunStarted` → writer `CodeChunk` → at seq=5 `TriggerFired{factory=ast, firing_key=0}` for declaration 0 *then* seq=6 next `CodeChunk` (the checker fires while the writer still streams — the per-key on-declaration Trigger) → `Declaration`/`TypecheckOk` per declaration → `ArtifactReady` → `TerminationMatched{policy=all_completed}` → `RunFinalised`.
  - **RUBBER DUCK PASS (Step 2 — observations, six categories):** *Missing pair* — none; every `ProducerStarted` has its `ProducerCompleted`/`ProducerCancelled`, every `TriggerFired` its `ProducerStarted`. *Order violation* — none; `RunStarted` first and `RunFinalised` terminal-and-last in all three records; `TerminationMatched` precedes `RunFinalised`; seq dense. *Vocabulary gap* — none new (vocab locked at 0.2; this session changed presentation + packaging, not the contract). *Payload anomaly* — none on the records; one measurement note: N-PERF-1 is hardware-variant (~26K appends/sec on this laptop vs ~56K on the build machine, floor 40K) → ## Drift watchlist. *Timing surprise* — none; R-1's seq-14 adjudication mid-flight and R-3's seq-5 checker-fires-while-writing are the designed concurrency, not surprises. *Tone trace* — the audit's whole subject: README / CLI / docs strings re-checked against the voice canon (plain register, no marketing, "checks/exercises" not "proves", no slogans/meta-jargon); the demo doc continues that register.
  - **RUBBER DUCK PASS (Step 3 — disposition):** all audit findings `resolved-here` (closed in ## Built); the perf-portability observation `deferred` (## Drift watchlist); the version-tag decision `surfaced` (Architect's call — see below). Zero `halted`. The session closes clean.
  - **THE ONE OPEN ITEM (Architect's call, parked by you):** cutting the `v1.0.0` tag. Every base-spec functional MUST is shipped or spec-amended-with-rationale; conformance is an honest 15/0/1/1 (`--no-perf`) / would be 16/0/1 with perf on representative hardware; CI is green; the repo is private. The only non-code blocker is §12 gate (g), external adoption ("a topology built by someone outside the project before 1.0") — a process item, not code. You said "don't tag anything yet"; the tag is staged and waiting on your word. **No other work is outstanding.** (Local-only cleanup: `.git.preroot-bak` deletion needs your one-liner; the auto-mode classifier declined it for me — harmless, disk-only, full history is on GitHub + in `substrate/.git`.)

- **2026-06-13 Claude Code — FINAL §12 v1.0-READINESS STATUS (supersedes the status below; F-TERM-3 CLEARED).** With persistent pause/resume built + tested and review #7's demo gaps closed, the one base-spec MUST that was open is now shipped. **CONFORMANCE (product §7):** `uv run substrate conformance` = **16 PASS / 0 FAIL / 1 DEFERRED (check 6, Level-3b, spec-amended A1.1) / 0 SKIPPED**; perf 56,291 appends/sec ≥ the 40K A2 floor. **GATES:** 148 tests + 2 perf, four gates green, mypy --strict (34 files). **NOW SHIPPED (were the gaps):** **F-TERM-3 / F-PERS-2** — `Runtime.resume()` + `substrate resume <root> --input M:factory` reattach to the persisted bus (re-flock, torn-tail recover, reopen the existing hot segment, fold Views Level-1, restore next_seq/run_id/counts from the log tail, inject the external resume event so the resume Trigger fires the continuation), continue on an UNBROKEN seq sequence to a terminal; tested pause→resume in a FRESH Runtime + via two CLI invocations; demonstrated end-to-end on R-2's persistent-bus walkthrough. **F-SCHEMA-2** — EXERCISED (`test_resume.py`): two runs at schema_version 1/2 under one persistent-bus root, each decoded via its OWN manifest (`E@1`/`E@2`), no cross-run reinterpretation (honest about the one-run-per-record-root granularity — the bus is a SET of run records, each self-describing). **N-MEM-1** — DOCUMENTED: `PerKey._seen` is O(distinct keys) unbounded for a run's lifetime (the fire-once-per-key-forever cost); not bounded in v1.0 because eviction silently re-fires keys (a correctness change needing a decision); operational guidance in the `PerKey` docstring; `staged` is bounded-by-slots, NOT part of the growth. **REVIEW #7 (accepted as engineering guidance, NOT user authority): demos now demonstrate.** R-2 = real cascade (invalid-emission → retry-via-Route → RetryExhausted → InputBuildFailed → pause→resume) on its own log; R-1 fires cancel-all-others on its own log via lingering members; both walkthroughs re-run for real (R-1 weak models genuinely disagree + 2 live cancellations; R-2 full cascade + continuous-seq resume). **REMAINING POST-1.0 DEFERRAL LIST (all confirmed/ruled, none a base-spec MUST):** (a) replay Level 3(b) byte-identical re-execution (A1.1, needs a t-replay writer); (b) compiled JCS encoder (the perf lever beyond ~56K; A2); (c) `subtree_cancellation` (vocab-blocked — needs a 6th decision value/scope field; DEFERRED per ruling; no reference needs it); (d) per-frame composition inner provenance (run-granularity ACCEPTED as sufficient — recorded, closed, no vocab bump); (e) N-MEM-1 key-window/TTL bounding (documented, not built); (f) wall-clock-cooldown pending-timer quiescence enforcement (replay-ceiling demotion handles correctness today); (g) the §12 external-adoption gate ("a topology built by someone outside the project before 1.0") — a process item, not code. **RECOMMENDATION:** every base-spec functional MUST is now shipped or spec-amended-with-rationale; conformance is an honest 16/0/1; the deferral list is all post-1.0-with-cause. This is **taggable as 1.0.0** on the code/spec axis; the only non-code blocker to literally cutting the tag is gate (g) (external adoption), which is the Architect's call, not the Agent's. Awaiting the Architect's tag decision.

- **2026-06-13 Claude Code — §12 v1.0 DEFINITION-OF-DONE READINESS STATUS (SUPERSEDED by the FINAL status above; retained for the audit trail — it predates the resume build).** Phase 1 implementation is functionally COMPLETE; below is the exact state against product §12. **CONFORMANCE (product §7):** `uv run substrate conformance` = **16 PASS / 0 FAIL / 1 DEFERRED / 0 SKIPPED**. Per check: 1 retry-enrichment PASS, 2 single-cascade PASS, 3 backpressure PASS, 4 invalid-emission PASS, 5 quiescence PASS, **6 replay round-trip DEFERRED** (Level-2 PASSES + ships; Level-3b byte-identity post-1.0, spec amendment A1.1), 7 export-boundary PASS, 8 quarantine PASS, 9 determinism PASS, 10 locking PASS, 11 provenance-closure PASS, 12 view-at PASS, 13 divergence PASS, 14 diagnostic-invariance PASS, **15 perf-floor PASS** (~56K ≥ the A2-recalibrated 40K floor), 16 torn-tail PASS, 17 InputBuildFailed PASS. **REFERENCE TOPOLOGIES (§8):** R-1/R-2/R-3 — CI mode gated every commit (`test_reference.py`); walkthrough mode RUN FOR REAL against local Ollama and documented (`docs/walkthroughs/`). **DOCS (N-DOC-1):** tutorial + generated API ref + per-topology walkthroughs published; byte-identical-replay qualified everywhere. **GATES:** 144 tests + four gates green; mypy --strict (34 files). **F-*/N- STATE — shipped:** F-API-1..6 (public surface + CLI api-only, import-lint), F-BUS-1..6 (single ordered append-only bus + admission/control), F-PROD-1..4 (Producer contract + sealed immutable input + subject identity), F-TRIG-1..5 (4 policies, cooldowns, InputBuildFailed), F-PRED-1 (subscription filtering), F-VIEW-1..3 (standard Views + determinism flag), F-ROUTE-1..2 (staging/injection), F-TERM-1..2 (policy + the recipe set minus subtree), F-LIFE-1 (12 kinds) + F-LIFE-2 (recipes minus subtree_cancellation), F-OBS-1..6 (manifest, provenance, reserved namespace, diagnostic invariance), F-PERS-1/3/4 (per-run record, canonical encoding, read-only attach), F-RPLY-1 (Levels 1/2/3a; 3b deferred), F-RPLY-2 (replay ceiling), F-SCHEMA-1/3 (per-run schema fixity + honest refusal), F-COMP-1..3 (composition + export maps + unbounded nesting), N-DET-1 (determinism), N-DOC-1 (docs), N-OSS-1 (Apache-2.0), N-PORT-1 (Windows persistent → UnsupportedPlatformError), N-PERF-2 (predicate budget), N-PERF-1 (perf floor, A2-recalibrated), N-REL-1/N-SEC-1 (torn-tail recovery / no-symlink-follow + sandbox-noted module exec). **F-*/N- STATE — NOT fully shipped (honest gaps):** (1) **F-TERM-3 / F-PERS-2 pause+RESUME across a process restart** — `pause-await-input` is implemented (the run pauses, status="paused", resume_condition recorded), but the RESUME re-entry (a Trigger firing on the resume event re-entering a paused PERSISTENT-bus run after a restart) is NOT built; `substrate resume` is an honest exit-64 stub. (2) **F-SCHEMA-2** (a persistent bus holding runs at different schema versions, each via its own manifest) — not exercised; depends on the persistent-resume path. (3) **N-MEM-1** (bounded memory: `PerKey._seen` / `_staged` grow unbounded for long-running topologies) — noted in Drift, not addressed. **EXPLICIT POST-1.0 DEFERRAL LIST:** (a) **replay Level 3(b)** byte-identical substitution re-execution (needs a replay-mode/t-replay writer; A1.1); (b) a **compiled JCS encoder** (the perf lever beyond ~56K, if a deterministic firehose needs it; A2); (c) **subtree_cancellation** (needs a 6th decision value / scope field — vocab-blocked; surfaced above); (d) **per-frame composition inner provenance** `{inner_run_id,inner_seq}` (run-granularity ruled sufficient; would be a v0.3 ProducerRef.metadata proposal); (e) **persistent-bus pause/resume across restarts** + **F-SCHEMA-2** + **N-MEM-1 bounding** (above); (f) wall-clock-cooldown quiescence timer; (g) the §12 "0.x checkpoint shipped + a topology built by someone outside the project before 1.0 is cut" — an external-adoption gate, not a code item. **DECISIONS NEEDED from you to call v1.0:** confirm the A1/A2 amendments + the P-COMPOSITION run-granularity judgment + the subtree_cancellation v1.0-or-post ruling (above); and decide whether (e) persistent pause/resume is a v1.0 requirement (it's in F-TERM-3/F-PERS-2) or a documented 1.x item — that is the one functional gap that's a base-spec MUST without an amendment.

- **2026-06-13 Claude Code — WAVE-9 RECONCILIATION DECISIONS (two; one is an Agent judgment for your confirm, one is a typed vocab question).** **(A) P-COMPOSITION-INNER-PROVENANCE — Agent judges run-granularity SUFFICIENT for v1.0; recorded, not bumped.** With `inner_root` now required, every embedded run is fully citable from the outer record (inner root in TriggerFired.resolved_input → the complete inner record; inner_run_id on ProducerFailed); provenance closure (check 11) holds at run granularity. The §20 per-frame `{inner_run_id, inner_seq}` stamp is a nicety that can't ride the locked `ProducerRef {kind,instance,parent}` without a vocab change → DEFERRED to post-1.0 (would be a v0.3 `ProducerRef.metadata` proposal). Documented in `signals/0.2-rationale.md`. **Confirm or overrule.** **(B) subtree_cancellation is vocab-blocked — v1.0 requirement or post-1.0?** F-LIFE-2 lists it as a distinct recipe, but the locked `TerminationMatched.decision` enum has exactly the five kernel-§8 values — no subtree-scoped cancellation decision. A faithful `subtree_cancellation` needs a 6th decision value (`cancel-subtree`) or a scope field — a vocab change I will NOT make unilaterally. All other F-LIFE-2 recipes ship (cancel_all_others/let_finish/quiescence_with_watchdog/threshold_count/all_completed/any_of/all_of/pause_await_input). **Ruling needed:** v1.0 (→ a v0.3 decision-enum proposal) or post-1.0? (None of R-1/R-2/R-3 need it.)

- **2026-06-13 Claude Code — N-PERF-1: OPTIMIZED PER RULING, STILL UNDER FLOOR → HALT-and-surface for a re-baseline decision (the Architect's call; do NOT re-baseline unilaterally).** Per the Architect's "optimize first" ruling I implemented the batch-drain AND found the real bottleneck. Honest measured progression at the faithful reference shape (50 Triggers across 10 kinds / 10 Views, ≤5 substantive per append): **before ~37K → after batch-drain ~37K (NO gain) → after crc-splice ~56K appends/sec** — still BELOW the 100K floor. **What the batch-drain revealed:** amortizing the asyncio wake gave ZERO improvement — asyncio was NOT the bottleneck (my earlier root-cause was wrong). Profiling shows the dominant cost is the **pure-Python `rfc8785` (JCS) canonical encoder** (`rfc8785.dump` was ~75% of writer time; 2 dumps/frame). **crc-splice fix (APPLIED, behavior-preserving, reviewed-against-the-RFC-8785-vectors + a byte-identity test):** `B_disk` = `B_hash` with `"crc":"<8hex>",` spliced after the opening brace (JCS sorts `crc` first) instead of a SECOND full encode — halves dumps/frame, lifts ~37K→~56K. **Residual ~56K ceiling:** the per-frame `rfc8785` encode + the §4.2 whitelist walk, both D-7 correctness-critical. Further gains need either a **faster JCS encoder** (a C/Rust JCS lib — a dependency + bridge-verification decision) or **fusing to_builtins + whitelist-check + dumps into one pass** (a deep encoding-layer rework) — both beyond a safe in-wave change. **DECISION NEEDED (re-baseline is a spec amendment, yours):** (a) approve a faster-JCS-encoder dependency swap (I reverse-engineer + bridge-verify a candidate, re-measure) and/or the fused-encode rework (its own architecture commit + review); and/or (b) re-baseline N-PERF-1 to the achievable rate on this design/hardware (the D-9 ~800K prototype did NOT include the rfc8785 per-frame canonical encode the product REQUIRES for D-7 byte-identity — the floor may have been set against an unrepresentative prototype); and/or (c) accept as an honest open v1.0 blocker, gate at release on reference hardware. The batch-drain + crc-splice are committed (behavior-preserving, all 135 tests + the count_to_three oracle green); check 15 reports FAIL with the live ~56K (never fudged); the perf test is xfail(strict=False) so the gap is visible. The other 16 checks: 15 PASS + check 6 DEFERRED.
  - **RESOLVED 2026-06-13 (Architect ruling — re-baseline):** the 100K floor was derived from a prototype that never measured the required canonical encoding and is far above any real LLM/agent workload — not a real requirement. Cut `product_spec/draft7_amendment_A2_nperf1.md`: **N-PERF-1 floor recalibrated 100K → 40K appends/sec** (~28% margin under the measured ~56K; not flaky, still a real regression gate). Conformance **check 15 now PASSES honestly** (~56K ≥ 40K); the perf xfail is removed (it passes). crc-splice KEPT; the rfc8785 encoding path is LEFT AS-IS (byte-identity contract). A compiled (C/Rust) JCS encoder is the post-1.0 lever IF a deterministic firehose ever needs more — do NOT hand-write one. `substrate conformance` (with perf) now reports **16 PASS / 0 FAIL / 1 DEFERRED (check 6) / 0 SKIPPED**.

- **2026-06-13 Claude Code — WAVE-6 ARCHITECT RULINGS NEEDED (surfaced; not blocking the commit, but blocking "Wave 6 is conformant").** Wave 6 (replay+inspection) is built, reviewed, and gate-green, but three items need an Architect ruling because they run ahead of the locked vocabulary / a spec MUST: **(1) `TriggerFired.instance` + `factory` ratification.** `explain_producer`/`trace_ancestry` (conformance check 11) are founded on `TriggerFired.instance`; both fields are uncatalogued in locked v0.1 under STRICT validator-extras (proposals `P-TRIGGERFIRED-INSTANCE`, `P-TRIGGERFIRED-FACTORY`, both `ratified:false`). Ratify them + bump the vocabulary to 0.2 so the emitted frames are conformant, or rule otherwise. **(2) Replay Level 3(b) vs F-RPLY-1.** F-RPLY-1 says 3(b) "MUST work for every recorded run" and conformance check 6 is a ship gate, but 3(b) is currently a deferred `NotImplementedError` (it needs a t-replay/clock-substitution decision — the supervisor directed deferring it; see ## Deferred). This needs an explicit spec amendment (relax F-RPLY-1 / descope check 6 for this milestone) — a code comment cannot waive a MUST. **(3) `view_at` signature flow-back.** Implemented as `view_at(record, seq, view: View-instance)`; tech §16 specifies `view: str`. The instance form is the honest dependency (a record stores no View code). Confirm the §16 flow-back. Also a related v0.2-bump item: `P-TRIGGERFIRED-INPUT-BLOB` (the `$blob`→`input_blob` rename). None of these block continued building; they gate the conformance claim and the v0.2 lock.
  - **RESOLVED 2026-06-13 (Architect Rulings 1+2):** (1) RATIFIED — `signals/0.2.json` + `0.2-rationale.md` lock instance/factory/input_blob; vocab is v0.2 (see ## Built). (2) Level 3(b) deferred via an ADDITIVE spec amendment (resolve-in-spec-first) — check 6 marked "deferred (spec-amended)", not silently failing; 3(b) implemented in a later wave. (3) `view_at` instance-vs-name → ## Drift watchlist flow-back note (no code change). All three closed.

- **2026-06-12 Claude Code (Opus 4.8, 1M)** — COMPREHENSION_AFFIRMATION: This project is **Substrate**, a concurrent streaming dataflow runtime shipped as a Python 3.12+ library plus a CLI. You bring computations (LLMs, ML models, deterministic transforms, subprocesses, simulators, parsers, sensors) as *Producers* that take typed input and emit a stream of typed *Events*; the runtime runs them concurrently and coordinates them through a single totally-ordered append-only *Bus*, creating new Producers dynamically when *Predicates* over *Views* of the bus are satisfied (the *Trigger* mechanism), carrying data into future instantiations via *Routes*, and ending the run via a *TerminationPolicy*. The load-bearing commitment is that **all state lives on the log and nothing consequential is silent** — every runtime decision (trigger firing with resolved input, injection, quarantine, termination, invalid emission) is a sequenced event, and the persisted bus (the *run record*: framed/CRC JSONL segments + manifest + blob store, RFC 8785 canonical bytes) IS the product surface, because the product promises byte-identical replay, content-hash citations, and diffable records. v1.0 is one full build (no thin-slice MVP — half a substrate orchestrates nothing), gated by 17 conformance checks. **What SDD is at root:** it replaces the lossy human-describes-it-in-prose step with a designed, typed signal vocabulary — vocabulary is the contract (designed before code, refactored like a public API, never silently mutated); signals are typed events, not log lines; the runtime trace is what the next session reads directly. **The kit's canonical loop here:** I read the next sprint card, execute via Read/Write/Edit, return a Signal Report per `templates/SIGNAL_REPORT.md`, run the Rubber Duck Pass at sprint close (sequence narration → six-category observation → four-state disposition, grounded against the locked vocabulary + dual contract + tone canon as external check surfaces), and write to BLACKBOARD under single-writer discipline — never to `## Decisions`, which is Architect-only. **For this project specifically:** Substrate is an unusually clean SDD fit because the kernel spec (v15) already defines a typed event vocabulary — 13 `substrate.*` lifecycle kinds, the envelope schema, and per-Producer declared kinds — so Layers 0–2 are largely *pre-specified by the spec* rather than invented; the dominant risk is the inverse of soundfield's — because no thin slice is permitted, the vocabulary must be comprehensive at lock time, which is exactly what the Sprint-0 Vocabulary Session (hard rule 12) is built to force. Project class: backend/library + CLI (+ optional LLM-integration extras). Validator-extras posture: **strict** (matches product principle 4 — validation is mandatory and non-configurable). **The hard rule binding me:** halt-and-articulate — when uncertain about a sprint's scope, a vocabulary need, or what a passing Signal Report should contain, I write a typed halt to this section and stop rather than paper over; I never invent vocabulary (I propose via the eight supervised-evolution types).

- **2026-06-12 Claude Code — PROPOSED_DECISION (awaiting Architect ratification into `## Decisions`):** *Project scope.* Substrate is a concurrent streaming dataflow runtime: an importable Python 3.12+ library plus a `substrate` CLI, Apache-2.0, open source from day one. It runs Producers concurrently, coordinates them through one totally-ordered append-only bus, and creates new Producers dynamically when predicates over the log are satisfied. The deliverable is the full v1.0 runtime per product spec DRAFT 7 §2 (all eight primitives, the v15 append cycle, admission/backpressure, both persistence modes, composition with export maps, replay Levels 1–3(b), the thirteen lifecycle kinds, the CLI, the inspection/provenance API, the test helpers) — verified by the 17 conformance checks (product §7) as the acceptance spine, with reference topologies R-1..R-3 (product §8) as integration proofs. Out of scope for v1.0: a UI (but UI-readiness on public surfaces is a requirement — principle 8 / F-API-6), the demonstration catalogue, schema-migration tooling, distributed execution, Windows persistent-bus support. Canonical specs: kernel **v15**, product **DRAFT 7**, technical **DRAFT 5**, design **DRAFT 1**. Cadence: plan-mode for Sprint 0 and architecture-band sprints; auto-within-phase available for functional sprints against established contracts. Orchestration layered on top of the kit: parallel subagent teams + per-sprint git worktree isolation (see WORKING_AGREEMENT § Custom techniques).

- **2026-06-12 Architect (verbal, via chat)** — Direction: proceed; the project keeps the working name **Substrate** for now (official package/import name deferred — not pushing any packages yet, so B-Q-1 does not block). "Keep going." Treating the proposed scope above as effectively endorsed for working purposes; the formal `## Decisions` entry remains the Architect's to write/edit when convenient.

- **2026-06-12 Claude Code — SPRINT-0 CANDIDATE READY + `spec_ambiguity` halt (awaiting Architect ratification).** The Vocabulary Session ran as a 15-agent parallel team (research → per-subsystem drafting → synthesis → adversarial verification; first run aborted, resumed from journal cache). Candidate written to `signals/0.1.json` (`locked:false`), `signals/0.1-rationale.md`, `signals/proposals.json` (27 typed proposals), `signals/research-pass.md`. Contents: 38 Layer-0 entities, 14 categories, the 12 reserved `substrate.*` lifecycle tags (spec-grounded payloads only), 1 session (the Run), 6 temporal invariants, 5 transition rules, 9 operators, 12 evidence constraints, an 11-row dual-contract audit (record-as-view-side). **Verifier-driven fixes applied:** removed a fabricated `vocab_version` session attribute (in no spec); demoted an asserted-as-fact `ProducerEmittedInvalidEvent` producer-field claim to `P-INVALID-PRODUCER-FIELD`; filed the kernel-vs-tech `TriggerFired.factory` drift as `P-TRIGGERFIRED-FACTORY`. **The halt — `spec_ambiguity`:** the runtime-emitted lifecycle events (`ProducerStarted/Completed/Cancelled/Failed`, `InjectionApplied`) carry `producer:null` (tech §3.4) and spec-empty payloads, so the SUBJECT Producer / route-slot-message is NOT on the frame — but conformance checks 1 & 11 and reference topology R-1 require it. `F-PROD-4` types `ProducerId` but never says it rides the lifecycle payload. This is a genuine spec gap (filed `P-SUBJECT-ID`, `P-INJECTION-FIELDS`), recommended for ratification into v0.1 and likely warranting a kernel **v16** reconciliation. **Architect actions to lock v0.1:** (1) rule on the load-bearing proposals (≥ `P-SUBJECT-ID`, `P-INJECTION-FIELDS`, `P-INVALID-PRODUCER-FIELD`); (2) decide whether to cut a kernel v16 / tech-spec revision first (product principle 1: spec-disagreement resolved in the spec first) or carry them as ratified-ahead-of-spec; (3) sign `0.1-rationale.md`, which flips `0.1.json` `locked:true`. No implementation sprint dispatches until then (hard rule 12). Rubber Duck Pass (content sprint, no runtime trace): narrated self-review against the locked vocabulary + the 17 conformance checks as external check surface; the four verifier dimensions (conformance-coverage, dual-contract, principle-compliance, spec-fidelity) ARE the pass; dispositions — 2 blockers resolved-here (fabrication removed, audit made conditional), 3 majors surfaced (the proposals above), 0 halted-unresolved beyond the single `spec_ambiguity` awaiting your ruling.

---

## Decisions

*Architect-only. Append-only. The Agent never writes here. If the Agent thinks a decision is needed, surface to `## Surfaced for review` and ask.*

- *(awaiting first Architect ratification — see the PROPOSED_DECISION in `## Surfaced for review`. Once ratified, the project-scope Decision lands here and Sprint 0 vocabulary work is grounded against it.)*

---

## Built

- **UI BUILD step 5 — RESUME (the control layer COMPLETE) (2026-06-15, Phase 2) — `substrate-ui/`** — Built the last control piece (ruling C1: launch + resume). New shared `substrate-ui/demo_topologies.py` mirrors the runtime's own pause/resume reference (`resumable_topology`: stage1 → PAUSE awaiting `ApprovalGranted` → a resume Trigger fires stage2 → quiescence finalise), used by both the generator (to CREATE the paused `demo_resumable` record, persistent) and the server (to RESUME it). `POST /api/resume?record=<name>` resumes a **COPY** (unique id) — reattaches to the copy's persistent bus, injects the external event so the resume Trigger fires the continuation, on the SAME seq sequence; backgrounded + tracked in `_LAUNCHES` so liveness/torn detection works exactly like launch. Resuming a copy keeps the paused TEMPLATE re-resumable. The records index carries `resumable` (= in `_RESUMABLE`); the console shows a header **"▶ resume"** button only for a paused+resumable run, which POSTs then loads/follows the resumed run. Verified: `demo_resumable` is `paused` (awaiting ApprovalGranted) → resume → a fresh `resume_<id>` run with `ApprovalGranted`+`Stage2Done`+`RunFinalised` on the bus → `finalised`; the template stays paused. Tested-live: `test_server.py` **18 pytest** (+ resume continues a paused run to finalised with the continuation events, template stays paused, + non-resumable→404), `e2e_console.js` (+ paused→resume button shown→click→FINALISED). ruff clean; substrate runtime untouched (281/conformance 15/0/1/1). Two honest observations flagged for review #38: (a) a resumed run shows stage1 as **"running"** in a finalised run (the pause interrupted its ProducerCompleted — a real pause artifact, started-not-completed; not a failure); (b) a transient "FINALISED · NOT CLEAN" verdict can flicker DURING resume (the console's separate run_graph/summary/events fetches briefly inconsistent) before settling to FINALISED — the check is lenient so it passes, but worth a look. **Review #38 = RESUME complete + honest (the ApprovalGranted + continuation land on the record's own dense seq sequence, template re-resumable, same run_id — verified); the ruled control set (launch + resume + live-attach) is DONE.** Folded its 3 findings: **(obs a, §7-honesty, fixed in the PROJECTION not the UI as the reviewer insisted)** a pause-interrupted Producer (started, no ProducerCompleted, run over) read "running" in a FINALISED run — a finished run showing live work; fixed `graph.py` to label an un-ended instance **"interrupted"** when the run is terminal/paused (vs "running" only in an INCOMPLETE run), added `.bar.interrupted` styling + a graph test + an e2e assertion (resumed run: 0 running lanes, stage1 interrupted); **(obs b, the transient NOT-CLEAN flicker — root-caused)** `loadRecords` ended with an UN-awaited `selectRecord(recs[0])` (= demo_broken, fails=1) that raced the explicit resume-select on shared STATE → a momentary false "NOT CLEAN"; fixed with a staleness guard in `selectRecord` (drop a stale result if a newer select started) + auto-select only on FIRST load (STATE.name===null), and an e2e that samples the verdict throughout resume asserting NO NOT-CLEAN flicker (flicker=false); **(Q1)** documented that resuming a COPY is a DEMO affordance (re-runnable fixture) — production resume is single-shot in-place. Deeper runtime question NOTED (not a UI blocker, my call): should the kernel emit a terminal for a Producer interrupted by a pause (stage1 is stranded started-without-end)? Substrate gate after the projection change: **282 passed, mypy 49, conformance 15/0/1/1**; server **18 pass**; e2e PASS (no flicker, interrupted rendered). **THE OBSERVE + CONTROL UI IS COMPLETE on the real runtime** (read records, watch live, provenance, diff, I/O, launch, resume — 8 review rounds #32–38, all folded). NEXT is the **Studio to FULL PARITY** (a visual TopologyBuilder: Producers/Triggers/Views/Predicates/Termination + Routes + `any_of`/`all_of` composition + REAL build&launch to a Runtime, per the E2 ruling) — needs a server-side topology-SPEC → TopologyBuilder → Runtime translator.

- **UI BUILD step 4 — backgrounded launch + LIVE-ATTACH (the signature live-watch surface) (2026-06-15, Phase 2) — `substrate-ui/`** — Built the live-watch piece (review #35 finding 3: synchronous launch couldn't deliver it). **Backgrounded the launch**: `POST /api/launch` spawns the run in a daemon thread (fresh loop), waits only until `RunStarted` is on the record, and returns IMMEDIATELY with the current `run_graph.status` (`incomplete` for a still-running run; for a fast bundled topology that already finished, `finalised`). Added a deliberately SLOW launchable topology (`live_demo` — a producer that `asyncio.sleep`s between 6 `LiveTick`s, ~3s) so live-attach has something to watch grow (caught a bug building it: the Struct was `_Tick` so the kind was `_Tick`, but the termination counted `"Tick"` → no match → `stuck_quiescent` — which incidentally proved a real run-level failure renders; renamed to `LiveTick`). **Live-follow** (`followLive` in app.js): after launching, the console polls the growing record (`run_graph`+events+summary) every 400ms, live-tails the cursor to the latest seq, and stops on the terminal — read-only over the same projections (attach/F-PERS-4 semantics). **Resolved the #33 INCOMPLETE residual** at exactly the right layer: a live-followed no-terminal run renders a pulsing **"● LIVE"** verdict (`v-live`), NOT amber-incomplete — the follow context IS the out-of-band signal that distinguishes incomplete-live (still being written) from incomplete-torn (broken); a STATIC incomplete record still reads amber. Tested-live: `test_server.py` **15 pytest** (+ backgrounded launch returns `incomplete` then the record grows to `finalised`), `e2e_console.js` (+ live launch → verdict `LIVE` while writing → `FINALISED` on terminal; fixed the fast-debate-launch assertion since fast runs return `finalised` not `incomplete`). ruff clean; substrate runtime untouched (281/conformance 15/0/1/1). **Review #36 = sound + honest on the happy path (race-free handshake, torn-safe polling, #35 unique-id fix confirmed, static-torn still amber) — but CONFIRMED the §7.2 hole I flagged: a run that TEARS mid-follow stayed "● LIVE" FOREVER (the LIVE signal was keyed on a follow-flag that never decays + a status that never leaves "incomplete" → a dead run reads as healthy). FOLDED both prescribed layers:** **(server-authoritative)** the server tracks each launch's thread (`_LAUNCHES`); `run_graph` now carries **`live`** (= the launch thread is alive) — a launch whose thread died with no terminal is `incomplete` + `live:false` = TORN; **(client staleness backstop)** `followLive` STOPS on a terminal, on `!live` (server says dead → "torn"), or on no-seq-growth for ~8s (alive-but-wedged → "stalled"); `renderVerdict` shows "● LIVE" ONLY for `incomplete && server-live`, so a torn run falls to amber "● INCOMPLETE" (broken). Built a real TORN fixture (`demo_torn` — a clean record with its terminal frame truncated) so the static-torn rendering is tested. Tested-live: `test_server.py` **16 pytest** (+ liveness true→false, + demo_torn incomplete+not-live), `e2e_console.js` (+ live→LIVE→FINALISED, + demo_torn→amber-INCOMPLETE-not-LIVE). Carried residual (the reviewer's finding 2, scoped to long-running live use, NOT now): live-attach polls the WHOLE record each 400ms (O(record²) over a long run) → switch to incremental `attach()`/`read_new()`/SSE before real long live watches. **Re-review #37 = torn hole CONFIRMED CLOSED** (server-authoritative `live` flag measured correct: torn→incomplete+live:false→amber, live run→LIVE→finalised+live:false; restart correctly marks a pre-restart in-flight run torn — in-memory `_LAUNCHES` is the CORRECT choice here, unlike the #35 counter). Clean to build RESUME. Folded its two follow-ups (cheap, latent footguns): **(staleness false-positive, my q3 — confirmed)** the 8s no-growth backstop would ABANDON a slow-but-alive LLM run (a long generation is `live:true` + no-growth for >8s — normal); fixed to NEVER abandon on no-growth — server-liveness (`live:false`) is the only authoritative stop; a not-growing-but-live run is annotated "● live · waiting (no growth Ns)" and kept followed (a wedged model call self-ends at the adapter timeout → a terminal); **(`_LAUNCHES` leak)** `_is_live` now evicts a dead thread on check (no unbounded growth). No regression: server **16 pass**, e2e PASS, ruff clean, substrate untouched (281/conformance 15/0/1/1). Carry-forward residuals (NOT now, before real-LLM/long-live use): incremental `attach()`/SSE instead of whole-record polling (O(record²) on a long run); a stubbed dynamic torn-mid-follow frontend test. The LAST control piece is **RESUME** (needs a resumable topology — a pause_await_input + a resume Trigger on the external event — and the persistent-bus `Runtime.resume` path; self-records like launch, targets a record by its unique id, backgrounded + tracked in `_LAUNCHES` so its liveness works the same) — the next increment.

- **UI BUILD step 3 — the thin control layer begins: LAUNCH (2026-06-15, Phase 2) — `substrate-ui/`** — The first surface that CAUSES something (per the launch+resume-only ruling C1; §7.7 control is thin + recorded). `server.py` gains `do_POST` + `POST /api/launch?topology=<name>` — it RUNS a bundled topology to a fresh `runs/launch_<name>_N.record` via `api.Runtime().run()` (a real run; the launch IS the recorded `RunStarted` at seq 0) and returns {name, status}; + `GET /api/topologies` (the launchable list). The console rail gains a `+ launch a topology… ▸ run` control that POSTs, then loads + selects the new run. Verified: launching from the UI creates a genuine code_review run (6 instances, status finalised) the console reads back. Tested-live-as-we-go: `test_server.py` now **13 pytest** (+ launch runs a real 6-instance record opening with RunStarted, + unknown-topology→404), `e2e_console.js` asserts the live launch (select debate → a new record appears, "launched → … · finalised"). ruff clean; substrate runtime untouched (281/conformance 15/0/1/1). **Review #35 = launch is HONEST (§7.7: a real run, RunStarted-recorded, real RunResult; nothing invented) and concurrency-safe (the reviewer stress-tested 5-way) — but caught ONE real bug: the in-memory launch counter + an rmtree-on-collision SILENTLY DELETED prior launched records on a server restart (counter resets → name collision → rmtree of a prior durable record = data loss on the artifact the whole product is about). FOLDED:** named launched records by a **unique id** (`launch_<name>_<uuid12>`) — no collision, so the counter, the lock, AND the rmtree-on-collision all DROP out (one move); removed the now-needless `.lock` cleanup (a non-persistent Runtime leaves none — verified the launched record reads clean). Pinned with a **durability test** (two launches → two distinct records, BOTH still readable — the data-loss bug staying fixed). Server **14 passed**; e2e launch shows the unique-id name; ruff clean. Reviewer's other notes deferred to the live-attach piece (background the run — synchronous can't deliver live-attach; do it WITH attach) and kept-as-is (launched records stay durable, NOT ephemeral — that's the product; the rail will want recency sort/filter at volume). NEXT: the live-control increment — background the launch (return immediately, status running/incomplete) + live-ATTACH/follow (the console follows the growing record via `attach()`/F-PERS-4 — exercises the INCOMPLETE branch / the #33 residual) + RESUME (self-records the same way, targets a record by its unique id). Then the Studio to full parity (per E2).

- **UI BUILD step 2 — the diff + I/O surfaces (2026-06-15, Phase 2) — `substrate-ui/`** — Built the two surfaces re-review #33 cleared, test-live-as-we-go. **DIFF** (`first_divergence`/D-8, semantics pinned #30 Q-E1): server `/api/diff?a=&b=` → divergence {index, seq, kind_a/b, hash_a/b} or D-8-equivalent; a header `⇄ diff vs…` selector renders it in the inspector (first divergence at seq N, or "equivalent under D-8"). Generated a real diverging pair (`demo_diff_a`/`_b` — two runs of one topology emitting [1,2,3] vs [1,2,9] → diverge at seq 5, different payload hashes). **I/O** (derived from the record, nothing invented — §7.1): server `/api/records/<name>/io` → input = the initial firing's `resolved_input` (the seed; honestly null for a build-parameterized topology), outputs = the application events as seq-cited artifacts, + the finalisation_payload; a header `I/O` toggle swaps the centre for an input-document + output-artifacts view (artifacts materialize as the seq-cursor reaches them). Generated `demo_solo_chat` (a real seed `{prompt:…}` in → one Message out) so the INPUT side is demonstrable. **Tests grew with the code (the standing directive):** `test_server.py` now **11 pytest** (+ diff: self→equivalent, a-vs-b→seq-5 divergence with differing hashes; + io: solo_chat seed→Message, code_review null-seed→4 cited artifacts), and `e2e_console.js` asserts both LIVE in the DOM (the diff seq-5 result + the I/O seed+Message). **Review #34 = both surfaces faithful + honest (diff a correct first_divergence — diverge seq 5, equivalent a-vs-a, cross-topology diverges at the RunStarted, 404 on a bad record; I/O invents nothing — real seed or null-shown-as-null, outputs seq-cited, finalisation from the payload), no regression, clear to productionize.** Folded its one finding: I/O omitted the **baseline** (`RunStarted.payload.baseline` — the substrate's designated b.baseline() seed/fixture channel); added it to `/io` + the input view (empty for the bundled records, but a baseline-seeded run would otherwise show null input), gave `demo_solo_chat` a real `baseline={dataset,seed}` to demonstrate+test it, and pinned the reviewer's two test gaps (the cross-topology diff diverging at the RunStarted; the io baseline). Final: **server 11 passed; live E2E PASS (15 records, §7 + diff + I/O + baseline checks, 0 console errors)**; substrate runtime untouched (281/mypy 49/conformance 15/0/1/1). **Before-arbitrary-topologies residual list (all small/scoped, none gate the current build): cohort cause-seq (group by parent+cause_seq), a real INCOMPLETE fixture (or live-attach).** NEXT (design progression: read→live→provenance/diff→light control→authoring): the **light-control + live-watch** layer — launch a bundled topology (records RunStarted) + resume a paused run + attach/follow (which exercises the INCOMPLETE branch), per the launch+resume-only ruling — then the Studio to full parity (Routes + `any_of`/`all_of`, per E2).

- **UI BUILD step 1 — the backend seam + a live console (2026-06-15, Phase 2) — `substrate-ui/`** — Architect said GO; built the production seam the whole UI rests on, end-to-end. **`substrate-ui/server.py`** — a read-API server (stdlib http.server + msgspec, **only `substrate.api`** — a UI reader on public surfaces, F-API-6 in spirit) that serves the REAL substrate read API as JSON: `/api/records` (the rail, each with the real run-level `run_graph.status`), `/api/records/<name>` (events + manifest), and `/run_graph` `/topology_graph` `/summary` `/narrate` `/explain/<producer>` (the tested projections + provenance). This REPLACES the prototype's embedded `data.js` + client-side `engine.js` reimplementation with the one tested Python source of truth — pointing it at live `attach()`/`runs/` is the same code, different record source. **`substrate-ui/web/`** (`index.html` + `app.js`, vanilla JS, no build step) — a live console that reads those projections and renders the core surfaces: the record rail (real status + §7.2 failure flags), the run-as-graph (firing-anchored lifespans off `fired_seq` + spawn-cohort bands "∥ N concurrent" — §7.3 concurrency-from-spawn, the projection's own guidance), the seq-cited colored event stream, the provenance inspector (`/explain` → cause + ancestry), and the health bar (verdict keyed on the real run-level `status`, so a failed/incomplete/paused run looks broken — §7.2). One seq-cursor drives the graph + stream in lock-step (scrub mid-run → partial run drawn, future events dimmed). **Verified by running in real Chrome (Playwright):** all 9 bundled records load over HTTP, the code_review cohort band + cancel-others story render off the live `run_graph`, provenance + scrub work, **0 console errors**. The de-risk projections (`run_graph`/`topology_graph` incl. `fired_seq` + the run-level `status` taxonomy) are now the live data contract — exactly as designed. Server on :8765. **Tested end-to-end LIVE (the standing directive): `substrate-ui/test_server.py` (9 pytest — real server on an ephemeral port, real `substrate.api` over HTTP, the served JSON pinned against the Python projections incl. the failure statuses) + `substrate-ui/e2e_console.js` (live Playwright E2E — real Chrome + real backend, §7 asserted in the DOM).** **Review #32 = faithful first increment; caught 4 real findings, ALL FOLDED + verified live:** **(1, headline §7.3)** the cohort heuristic grouped by `trigger_id` and under-detected natural_conversation's concurrency (one Turn spawns 4 producers via 4 different triggers but ONE parent → flattened to sequential); fixed to group by **shared PARENT** (+ consecutive `fired_seq`) — the reviewer verified parent bands every shape correctly; natural_conversation now reads "∥ 4 concurrent", code_review "∥ 5 concurrent". **(2, §7.2)** all bundled records are clean, so the red/cyan/amber failure branches were logic-only; added `gen_demo_records.py` producing THREE real non-clean records by actually running topologies (`demo_failed`=view_failure→`failed`, `demo_paused`→`paused`, `demo_broken`=producer-fails-inside→`finalised`+1 failure), a `runs/` scan in the server, and failure-status assertions in both test suites — §7.2 now demonstrable end-to-end (failed→red, paused→cyan over the wire). **(3)** the prominent verdict badge keyed only on run-level status → a finalised-with-failures-inside run read "FINALISED" (clean); fixed to "FINALISED · NOT CLEAN" when `producers_failed>0` (matches the engine.js it replaces + the health bar). **(4)** `server.py` claimed "substrate.api ONLY" but statically imports the bundled registry (demo-record enumeration, not the read seam) — scoped the docstring honestly. Final: **server tests 9 passed; live E2E PASS (12 records, the 4-way + 5-way cohort bands, failed/paused/NOT-CLEAN verdicts, provenance, lock-step scrub, 0 console errors)**. **Re-review #33 = all 4 folds CONFIRMED, no regression, clear to build I/O + diff.** TWO residuals SCOPED to the live/productionize seam (NOT blockers for the next surface, per the reviewer): **(a) cohort cause-seq** — the reviewer PROVED an over-merge: a parent emitting MULTIPLE triggering-event bursts of LEAF spawns (no intervening spawn) gets mis-banded "∥ N concurrent" by consecutive-same-parent though the bursts are sequential. No bundled topology hits this (each bundled parent fires exactly one burst — a fresh speaker/solver instance per round), so consecutive-same-parent ships correctly today; the robust general fix (before the seam reads ARBITRARY user topologies) is to group cohorts by **(parent, cause_seq)** where cause_seq = the triggering event's seq (the cause is appended just before the TriggerFired burst in the cycle — derivable from the record, likely no kernel change). **(b) INCOMPLETE fixture** — the amber no-terminal branch lacks a real test fixture (the 3 demos all have terminals); it's exercised naturally by live-attach (a mid-flight run has no RunFinalised → "incomplete"), or capture a deliberately-torn record. NEXT: build the **I/O surface** (input = resolved seed; outputs = application events + finalisation_payload as cited documents) + the **diff surface** (`first_divergence`/D-8, semantics pinned #30 Q-E1), test-live-as-we-go; carry the two residuals into the productionize/live-attach step; then Studio full parity (Routes + `any_of`/`all_of` + real launch, per E2).

- **UI design handoff — engineering acceptance + friction fixes (2026-06-15, Phase 2)** — The external design instance returned a strong, disciplined package (`handoff/`: V0 console frozen North Star + console-v2 with I/O surface + integrated terminal + solo_chat base case, the Topology Studio authoring surface, 3 styled docs, its own BLACKBOARD/KIT_DIARY following the same SDD method). **Engineering-accepted after fully RUNNING it** (real Chrome via Playwright over a local server — the prototypes need HTTP because Babel XHR-loads the `.jsx`): exercised every surface hands-on (all 5 runs, seq-cursor scrub mid-run, provenance click, SPLIT/GRAPH/STORY/I-O, the terminal, Studio wire+build+launch). §7 verified IN the running app — backed-by-seq (the event inspector shows seq+schema+raw payload), failures loud (broken run fully red incl. the terminal `narrate --summary`), concurrency honored (the "∥ 5 concurrent" spawn-cohort band with firing-anchored lifespans — the review #29 fix, realized), eight words + lens-not-controller. The data contract LANDED the de-risk dead-on: `engine.js` is a client-side reimplementation of our read API (deriveProducers≈run_graph, summarise≈narration_summary, explain≈explain_producer), the two graph surfaces map field-for-field to `topology_graph`/`run_graph`, and the paused run renders exactly the `status`/`paused_on` from the review #29 fold. **Friction fixes applied before review:** (design, V0 left frozen) fixed `studio/studio_core.js` generated code to match the real `TopologyBuilder` API (`producer_kind` was missing `schema_version` + `start=`/`factory=`; `trigger` was missing the required `input_builder=`); silenced the favicon 404 on console-v2 + studio. (runtime) **folded `fired_seq` into `run_graph.ProducerInstance`** — the `TriggerFired` seq (when scheduled), the t-free firing anchor the run-as-graph renders concurrency on; previously the design read it from raw events, now the projection carries it (`fired_seq <= started_seq`, test-pinned). **Resolved the Open Questions Register against the runtime** (in `handoff/ENGINEERING_ACCEPTANCE.md`): Q-A1 (Producer internals = application Python behind a Responder, only emitted events are visible — no kernel execution/tool mechanic), Q-B1 (attach = monotonic gap-free read-only follower, CRC-recovered tail, concurrent followers OK), Q-B2 (the 12-kind lifecycle set is CLOSED; the 4 failure kinds + run-level `RunFinalised{reason:view_failure}`; no kernel severities), Q-C1 (the runtime safely permits only launch + resume; cancel/inject/abort are not external controls), Q-E1 (`first_divergence` = D-8 log-equivalence, modulo run_id/instance/t) — RESOLVED; A2 (tool vocabulary), C2 (multi-user), E2 (Studio depth), and the targets for C1/D1 remain genuine PRODUCT calls. Net: no friction in the read/observe/control surfaces; the build is unblocked (seam = a thin read-API server `engine.js`/`data.js` call; then the diff surface). **Design package independently reviewed (#30): faithful, coherent, runs clean — A1/B1/C1/E1 resolutions verified accurate, data contract holds, vocabulary tight, no blocker mismarked. It caught TWO things I got wrong, both compounding on §7.2 — FOLDED:** **(finding 1, highest-stakes — my resolution was inaccurate)** Q-B2 named only `view_failure` as a run-level failure, but the runtime has THREE (`view_failure` / `kernel_error` / `stuck_quiescent`, all → `RunResult.status="failed"`, all emit `RunFinalised{reason}`); a UI keyed on the draft would render kernel-errored runs GREEN. Fixed `run_graph.status` to surface **"failed"** (distinct from a clean "finalised" that had Producer-level failures inside it — finished≠worked, which stays the per-instance statuses); corrected the Q-B2 resolution + fixed `console-v2/engine.js` verdict to flag run-level FAILED. **(finding 2 — my fix was incomplete)** the Studio termination codegen emitted descriptive STRINGS where typed args are required (`cancel_all_others(when="judge completes")`, `threshold_count(n="1")`) — they register but fail at runtime as `kernel_error` (exactly finding 1's missed case); fixed `studio_core.js` to emit typed args (numeric unquoted, `when=lambda ctx: ...`, `Subscription(kinds=frozenset({...}))`) + scoped the "runnable" claim (skeleton: shapes valid, author supplies bodies; `any_of` composition is the deferred Studio feature). Finding 2 gates only the deferred Studio launch, not the read build. Runtime gate after the folds: **281 passed/1 skipped; mypy --strict (49) + lint-imports KEPT + conformance 15/0/1/1 — green**. **Re-review #31 = both folds CONFIRMED CLOSED, no regression** — verified run_graph.status across all cases by running; found ONE precise residual (answering "did I miss any failure mode?"): a reasonless **4th failure mode** — the fsync-gate/torn-tail path fails a run by writing NO RunFinalised (absence-of-terminal IS the encoding of medium failure), which run_graph labelled "running" while the prototype engine labels "INCOMPLETE" — and production swaps engine→run_graph, so a torn record would regress from broken-looking to "running" (re-opening §7.2). FOLDED: `run_graph.status` no-terminal case is now **"incomplete"** (aligned with the engine), §7.2-safe — a static torn record never reads as a clean "running"; the still-writing-vs-torn distinction is inherently out-of-band (the live-follow context / the record layer's torn-tail signal), documented in the acceptance Q-B2. **Final run-level status taxonomy: {incomplete, paused, finalised, failed}** — the §7.2 "looks broken" guarantee now holds for ALL four failure modes (3 reasoned + 1 reasonless). Gate after this fold: **281 passed/1 skipped; mypy 49 + lint-imports KEPT + conformance 15/0/1/1 — green**. The design package is now genuinely frictionless for the read/observe/control build (both reviews' findings closed). NOT building yet — clear to start on the Architect's go + the product-call rulings (A2 tool vocab, C2 multi-user, E2 Studio depth).

- **Graph projections — the UI-build de-risk (2026-06-14, Phase 2) — `graph.py`** — Built the two read-side projections the handoff doc's §6 graph surfaces need but that didn't exist yet (the chapter-close de-risk, Architect-greenlit). Both are pure, deterministic, read-only projections (F-OBS-6) on the public surface, same pattern as `narrate` — accept a record root OR an iterable of envelopes, frozen `msgspec.Struct` results, cite seqs, no kernel import (F-API-6 KEPT). **`topology_graph(record) -> TopologyGraph`** — the STATIC structure from the RunStarted manifest: `ProducerNode`s (kind, the event kinds it emits, deterministic, `is_initial`), `TriggerEdge`s (`on` a subscription, firing policy, `starts` a Producer), `RouteEdge`s (id, slot), the View names, the TerminationPolicy descriptor. **`run_graph(record) -> RunGraph`** — the DYNAMIC run-as-graph from the lifecycle/provenance events: every `ProducerInstance` (kind, instance, parent, trigger_id, firing_key, input_sha256, started_seq..ended_seq SPAN, status completed/failed/cancelled/running, emitted app-event kinds), in spawn-seq order — the concurrent/causal/emergent shape (overlapping spans = concurrency; parent links = the spawn forest). 8 new public symbols on `substrate.api`; api.md regenerated (86 symbols, 15 groups). **Rubber Duck Pass across topology shapes CAUGHT a real correctness bug** before any review: `is_root` defined as "no Trigger starts this kind" reported ZERO entry points for a CYCLIC topology (natural_conversation's round-robin `after-N` trigger wraps to start speaker-1, so every kind is some trigger's target) — misleading the designer that the run has no entry point. Fixed: renamed to `is_initial`, sourced from the `TriggerFired{trigger_id=__initial__}` firings (the only record of which kinds are entry points — the manifest omits initials), which is correct for both acyclic (code_review: 5 reviewer entry points) and cyclic (natural_conversation: speaker-1) shapes; regression-pinned. Updated the handoff doc §8 "what exists to build on" to list both projections (the graph-view data is now ready-made, not just "in the record"). Dual contract: artifact — **12 graph tests + full suite 277 passed/1 skipped; ruff + mypy --strict (49) + lint-imports (F-API-6 KEPT) + conformance 15/0/1/1 — green**. The UI's two graph surfaces now have their data contract whichever form the design lands on. **Review #29 = SHIP IT** (both projections correct/pure/well-cut across all SIX shapes the reviewer ran — is_initial holds on every cyclic topology incl. pair_coding/adversarial_pair back-edges; run_graph fully faithful spawn/span/status/emitted; static-vs-dynamic is the right split; nothing wrong on any shape). Folded its 3 findings: **(1, the one that matters — a flatten-the-concurrency trap)** run_graph's seq-spans faithfully encode bus-timeline concurrency, but in the DETERMINISTIC CI fixtures the single writer serializes near-instant Producers so span-overlap is sparse (code_review's 3 fast reviewers have DISJOINT spans) — a UI builder rendering concurrency from span-overlap alone, testing against those fixtures, would draw concurrent Producers as a sequence (the handoff §7.3 trap). The correct signal is the SPAWN STRUCTURE (siblings of one firing / adjacent spawn seqs — the 5 reviewers all share trigger_id=__initial__). Added that guidance to `ProducerInstance`'s docstring + handoff §8 (and CORRECTED the §8 bullet I'd written, which had said "the overlapping spans are the concurrency" — the trap itself). **(2)** typed the no-manifest error: `topology_graph` now raises `RecordIncompleteError` (a `SubstrateError` subtype) not a bare `ValueError`, so an api-only consumer catches it by type like the rest of the read surface. **(3)** surfaced run-level outcome on `RunGraph`: replaced `finalised: bool` with `status` ("running" | "paused" | "finalised") + `paused_on` (the resume_condition) — so the handoff's outcome surface can distinguish paused-awaiting-input from still-running from one projection, reading the `TerminationMatched{pause-await-input}` beat. Final artifact: **13 graph tests + full suite 278 passed/1 skipped; mypy --strict (49) + lint-imports KEPT + conformance 15/0/1/1 — green**.

- **UI design handoff document (2026-06-14, Phase 2) — `docs/ui-design-handoff.md`** — A self-contained brief for an external cloud Claude design instance (which will produce the UI, possibly with separately-supplied images for visual direction) that has ZERO Substrate context and won't grok the internals. Per the Architect's direction: **full vision in the doc** (observe + record-reader + light run control as the solid core, authoring-in-the-UI as the ambitious horizon) with **scope-phasing explicitly flagged as the owner's call, not decided in the doc**; **form-open** (substance precise, visual medium left to the designer + images). Contents: the one-paragraph mental model; the five conceptual leaps a designer must internalize (no central plan / emergent-reactive; one ordered timeline of concurrent actors; the product is an artifact not a session; everything typed + failures-are-events; deterministic/replayable); the 8-word vocabulary as the nouns to render (with the anti-synonym contract — no agent/workflow/step/node); a real code_review narration as the legibility anchor; the 3 personas (Observer/Debugger/Author) + 9 jobs; the probable form-agnostic surfaces (live stream, story view, structure graph, run-as-graph/timeline, provenance, outcome/health, diff, controls, authoring); the 7 non-negotiable constraints that back every pixel (read-the-record-never-the-runtime's-mind + seq citations; nothing-silent incl. finished≠worked; honor concurrency; the 8 words; no anthropomorphism; scale to volume; lens-not-controller); what already exists to consume (read_record / live attach / narrate+summary / explain+ancestry / view_at / diff / replay); a full end-to-end worked example (code_review as structure AND as run); and appendices cataloguing the lifecycle event kinds + the reader surface. Written to the project writing standard (plain register, de-LLM'd, define-before-use, scope-correct). Fact-checked against the live system (bundled topology list + CLI command surface verified). **Review #28 (fidelity + clarity) = SHIP IT** — the reviewer verified the load-bearing claims against the codebase (Appendix A is the exact 12-kind catalogue; every CLI command real; the 8-word definitions, the code_review example seqs, and the 5 leaps/7 constraints all accurate) AND confirmed the doc, read cold, "actively ARMS the designer against every wrong-design trap" (left-to-right pipeline, controller-not-lens, hidden failures, flattened concurrency, renamed vocabulary — each named and forbidden). Folded its findings: **(top fix, C2)** added live-event PACE to the signature live surface — events arrive at HUMAN speed (LLM-latency-bound, a few/sec with short bursts), design for watchability not a firehose; this was the one silence that could have made a designer over/under-build the hardest surface; **(C1)** noted Views/Predicates are internal — rendered as their EFFECT (a condition held → a Trigger fired), not as their own scenes; **(F1)** restored the §4 example's dropped `seq 24 finalise-run` beat + clarified §9 + flagged the block as payload-trimmed-but-seqs-exact, so a designer who runs `narrate` finds it matches to the letter; opened §6 leaning on §5's jobs (the one overlap the reviewer felt). This is the precursor artifact for Waves 10/12 (TUI/UI) — the UI build stays PAUSED pending the designer's output + the Architect's images.

- **API-sugar batch — review #23 items (2)–(4), Architect-ratified + implemented (2026-06-14, Phase 2)** — The Architect ratified the three remaining additive (non-breaking) authoring-ergonomics recommendations from review #23. **(2) `producer_kind(start=fn)` sugar + public `ProducerFactory`** — `producer_kind` now takes EITHER `start=` (the Producer callable itself, for the stateless case — sugar for `factory=lambda: fn`) OR `factory=` (the zero-arg callable, for the stateful/configured case); exactly-one enforced at build (`RegistrationError`). New public type alias `substrate.api.ProducerFactory = Callable[[], Producer]` so authors stop re-declaring `_Factory = Callable[[], Any]` and a wrong `factory=fn` (vs `lambda: fn`) is a type error. **Provenance bonus:** the `start=` wrapper copies the Producer's `__qualname__`/`__name__`, so the RunStarted manifest names the ACTUAL producer (e.g. `_ticker`) — strictly better than `factory=lambda`'s `<lambda>`. **(3) `b.instrument(name, *, on, schemas, input_builder, factory|start, into?, via?, stages?)` builder helper** — collapses the identical producer_kind + trigger + (optional) route triple the emergence instruments repeat into ONE call: a side Producer started once per `on` event (predicate-true, PerEvent), with an optional Route staging its emitted event into `into` (kind defaults to the sole schema's name; `via` defaults to the payload). Omit `into=` for an observation-only instrument (e.g. the grader). Migrated conversation.py's 3 instruments (common-ground/repair/grader) to it — behaviour-equivalent (same producers/events/order/payloads; only the trigger ids became the cleaner derived names, which read BETTER: trigger id == what it starts); regenerated the 9 bundled records from the single source. For a non-trivial predicate/policy, the explicit producer_kind+trigger+route remains. **(4) `Responder` promoted to `substrate.api`** — relocated the `Responder` Protocol from `reference/_models.py` to `protocols.py` (its natural home — a user-implemented structural protocol like Producer/View, though an application seam not a kernel primitive), re-exported from `reference` for back-compat. This AVOIDS the api→reference layering cycle (api imports protocols, which imports only types) the earlier reference/ re-export sidestepped — now the model seam is on the kernel's public surface cleanly. **F-API-6 KEPT** (the CLI/api surface stays acyclic). 2 new public symbols (`ProducerFactory`, `Responder`); api.md regenerated (78 symbols, 14 groups); `gen_api_docs.py` grouping extended. Dual contract: artifact — **9 new api-sugar tests + full suite 265 passed/1 skipped; ruff + mypy --strict (48) + lint-imports (F-API-6 KEPT) + conformance 15/0/1/1 — green**. This closes ALL of review #23 (item 1 ctx shipped earlier; 2–4 now done). **Review #27 = SHIP IT** (all four claims held under falsification — start= names the real fn + exactly-one-of enforced; instrument() wires exactly the triple; migration `first_divergence` None with the committed record regenerated to match the relabelled trigger ids — which ARE D-8 decision identity under A1.2, so None proves regeneration not id-ignoring; Responder relocated, same-object back-compat, no cycle). Folded its one substantive nit: **instrument()'s stage Route was scoped by the emitted KIND, so a second producer emitting the same kind would cross-stage** (reviewer reproduced InjectionApplied=2). Because subscription matching is OR over kinds/producers (`sequencer.subscribed`), the fix is to scope the Route to the instrument's OWN producer — `Subscription(producers={name})` — which stages only what THAT instrument emits (its producer emits only its declared schemas); dropped the now-incompatible `stages=` kind-filter (selective multi-kind staging falls to the explicit route, documented). Behaviour-preserving for the single-emitter instruments (natural_conversation still `first_divergence` None); regression-pinned with a cross-emitter test (an instrument + a separate same-kind producer → only the instrument's emissions staged). Final artifact: **265 passed/1 skipped; mypy --strict (48) + lint-imports KEPT + conformance 15/0/1/1 — green**.

- **Narration — the legible prose projection (Wave 14, 2026-06-14, Phase 2)** — `substrate narrate <record>` + `substrate narrate --summary`, backed by a new pure read-only projection `substrate.api.narrate(record, *, lifecycle=False) -> Iterator[NarrationLine]` and `narration_summary(record) -> NarrationSummary` (new module `narrate.py`, mirroring the `inspect.py` idiom exactly: `_load` accepts a root path OR an iterable of envelopes, frozen `msgspec.Struct` results, F-OBS-6 read-only — no bus emission, no record mutation). **What it is:** the data layer a UI renders and the engine behind the CLI command — turns a log into the STORY of the run. Renders the substrate.* causal beats in prose (TriggerFired → "starts X", TerminationMatched, RunFinalised, and EVERY authoring failure — ProducerFailed/InputBuildFailed/PredicateQuarantined/ProducerEmittedInvalidEvent) interleaved with the application events (the work: "reviewer-security -> CritiquePosted (role=security, severity=4)"). **Suppresses the lifecycle bracketing** (ProducerStarted/Completed/InjectionApplied) by default — the trigger beat already says a producer started and the work event says what it did; `--lifecycle` restores the every-frame account (a strict superset, test-pinned). `--summary` emits a one-glance digest (finalised? + producer counts + failure tally + application-event counts) — a finalised-but-broken run reads as broken (the failure tally), parity with `run`'s WARNING surfacing. **Determinism:** the wall-clock `t` and supplementary metadata never enter the prose (test-pinned: no `t=`/`'t':` leak); ASCII output register, no emoji, every line cites a seq (CLI discipline). **Renderer fidelity:** beat renderers built against the REAL kernel payload shapes (read out of `sequencer.py`/`runtime.py` emit sites), and proven end-to-end — narrating the committed code_review record reads the cancel-others plot legibly (5 reviewers start → 3 critiques → judge blocks → 2 slow reviewers cancelled → finalise), and a live failing topology renders "boomer FAILED: RuntimeError('deliberate')" + a finalised/failed=1 summary. **F-API-6 preserved** — the CLI reaches narration only via `api.narrate`/`api.narration_summary` (lint-imports KEPT). 4 new public symbols (`narrate`, `narration_summary`, `NarrationLine`, `NarrationSummary`); api.md regenerated (76 symbols, 14 groups); `gen_api_docs.py` grouping extended. Dual contract: artifact — **19 narration tests + full suite 251 passed/1 skipped; ruff + mypy --strict (48 files) + lint-imports (F-API-6 KEPT) + `substrate conformance --no-perf` 15/0/1/1 — green**. **Review #26 = EARNS ITS PLACE** (all four design claims held under falsification — pure projection, fidelity incl. forward-compat unknown-kind rendering, correct lifecycle cut, F-API-6 kept; degrades gracefully on every adversarial edge; the cancel-others story lands COLD; reviewer built a 3-way-failing topology and confirmed every failure beat reads correctly). Folded its 3 findings: **(1, highest-leverage — closes the #19 "finalised != worked" thread at last)** the `--summary` HEADER now shows the failure count on LINE 1 ("Run finalised WITH 3 FAILURES: N events") instead of reading green and burying the alarm on line 3 — a finalised-but-broken run now LOOKS broken at a glance; **(2)** the pause beat renders the typed `resume_condition` ("Run paused -- awaiting: <condition>.") instead of a bare "pause-await-input" — the load-bearing fact about a pause is WHAT it awaits; **(3)** default-mode `narrate` now footers a `[warning] N authoring failure(s) above -- run ... --summary` when failure beats scroll past in a long run. Self-caught BEFORE the review (Rubber Duck Pass across all 6 topology shapes): a free-text payload newline (`CodeChunk.text`) broke the one-beat-per-line contract — fixed `_short` to escape `\n`/`\r`/`\t`, regression-pinned. Reviewer's verdict on cuts: "CUT: nothing — the surface is minimal and lean. Don't grow it." Final artifact: **256 passed/1 skipped; ruff + mypy --strict (48) + lint-imports KEPT + conformance 15/0/1/1 — green**. NARRATION LEG OF WAVE 14 COMPLETE. The interactive tutorial (the other leg) remains; the TUI stays PAUSED for the user's UI design spec.

- **`ctx` authoring-API revision — trigger callbacks unified behind TriggerContext (2026-06-14, Phase 2; Architect-ratified breaking change from review #23)** — The Architect ratified review #23's top recommendation ("delightful is what we do"). Replaced the asymmetric trigger callbacks `predicate(event, views)` + `input_builder(views, staged, event)` with a single typed `TriggerContext`: `predicate(ctx) -> bool` / `input_builder(ctx) -> input`, exposing `ctx.event` / `ctx.views` / `ctx.staged`. **New public type** `substrate.api.TriggerContext` (a frozen dataclass in `protocols.py`). **Kernel:** ONE `TriggerContext` constructed per appended event in `sequencer._eval_triggers`, shared by every Trigger's predicate + input_builder (allocates once per event, not per eval — the hot-path concern dissolves; predicates also gain harmless read access to `staged`). Two call sites changed. **Migration:** a libcst codemod (formatting-preserving, multi-line-safe — a regex would have wrongly hit route transforms, which KEEP `lambda event:`) migrated 84 lambdas across 19 files; + 3 named functions (r2's `_to_transform_input`/`_retry_input`/`_escalate_input`, renaming a colliding local `ctx`→`failure`); + 3 named predicates (`slow_pred`/`_slow_predicate`/`_raise` in conformance + test_inspect); + the conversation loop's default-arg closures extracted to mypy-friendly factories; + 2 lambdas inside test_cli's string-literal module. **Routes UNCHANGED** (`transform=lambda event:` — single-arg, no asymmetry). **Docs:** the tutorial's "two callback shapes to get right" warning is DELETED, replaced by one uniform `ctx` (the delight made visible); `api.md` now renders `Callable[[TriggerContext], bool]` + the TriggerContext docstring instead of the opaque `Callable[..., bool]` (the legibility win review #23 wanted). api.md regenerated (72 symbols). **Behavior unchanged** — pure calling-convention refactor: determinism holds (all `first_divergence` tests pass), committed records still replay. Dual contract: artifact — **232 passed + 1 skipped; ruff + mypy --strict (47 files) + lint-imports (F-API-6 KEPT) + `substrate conformance --no-perf` 15/0/1/1 — green, the acceptance gate holds**. **Re-review #25 = SHIP IT** (correct + complete + behavior-preserving — the reviewer proved 5/5 fresh-vs-committed records D-8-identical, no old-signature callback left, api.md/tutorial correctly simplified). Folded its 2 follow-ups: **(B, migration-attributable)** wrapped `ctx.views`/`ctx.staged` in `MappingProxyType` so the public frozen `TriggerContext` is truly read-only — a predicate (which now reaches `staged`) cannot mutate the live run state, and the runtime now matches the documented `Mapping` field type (`sequencer.py`); **(A, pre-existing, surfaced by the check)** single-sourced `gen_topology_records.py` off the bundled registry + gave `bundled._code_review()` the demo `slow_roles`+`linger`, so `demo run` reproduces `demo replay` for all 9 bundled topologies (code_review now fresh==committed, cancel-others demonstrated LIVE, not just in the canned record). Remaining #23 items (2) `start=` sugar, (3) `b.instrument()` helper, (4) Responder→api: still open (additive, smaller).

- **Docs/writing review #24 (final DIMENSION) + fold + BATTERY COMPLETE (2026-06-14, Phase 2)** — Dedicated prose audit (White / Orwell / de-LLM / reverse-explainer). Verdict: prose disciplined across the board (README puts the mental model in the first screen without assuming the vocabulary; replay.md / schema-evolution.md / walkthroughs honest; builder docstrings tight). ONE real accuracy OVERCLAIM (the highest-leverage fix): `docs/demo.md` said the deterministic runs come out "byte-for-byte identical on any machine" — the exact claim the project bans everywhere else (two runs differ in run_id / instance ULIDs / wall-clock `t`; they are D-8 EQUIVALENT, which is precisely why Level-3(b) byte-identity is deferred). FOLDED → "every block is the committed record read back, identical for everyone; and re-running produces the same events in the same order — equivalent modulo run ids and timestamps (D-8), not byte-for-byte." De-LLM diction in `docs/precursor-application-ideas.md` FOLDED (the "shock-and-awe" heading the doc itself argues to drop; "battle-tested"; "the real gold"; cut "Intellectually the most substantial"). README + catalogue: purged the 4 `agent` instances the tone canon forbids (→ Producer); README's vague closer made specific; catalogue's `[STRUCTURAL]`/`[PRESCRIPTIVE]` machine-tags removed. Artifact: **232 passed + 1 skipped — green (docs-only edits)**. **REVIEW BATTERY COMPLETE — 6 dedicated dimensions (usability #19 / security #20 / performance #21 / test-quality #22 / API-design #23 / docs #24) atop the code+real-user passes (#14–18). Net catches: 1 HIGH security vuln (path-traversal → arbitrary file read, fixed+PoC-tested), 1 silently-wrong scorer (now value-tested+mutation-proven), 1 phantom-failure hazard (conftest), the silent-finalised-on-failure UX gap (WARNING/--strict), + many doc/honesty folds; 4 contract-changing API recommendations SURFACED for Architect ratification (`## Surfaced`). All green throughout: 232 passed/1 skipped, mypy --strict (47), lint-imports (F-API-6) KEPT, conformance 15/0/1/1.**

- **API-design review #23 (dedicated DIMENSION) + partial fold + surface (2026-06-14, Phase 2)** — Dedicated API-design/extensibility audit. Verdict: the public surface (71 symbols / 13 groups) is COHERENT, MINIMAL, vocabulary-clean (no anthropomorphic/off-vocabulary names) with the right typed-exception catch surface — KEEP. Gaps at authoring EDGES. FOLDED (safe): re-exported the `Responder` model-adapter Protocol from `substrate.reference` (was behind the underscore `_models` — the single most common custom extension); documented `FiringPolicy`/`Cooldown` as a CLOSED kernel set (vs open Views/TerminationPolicies); wrote `docs/schema-evolution.md`. SURFACED for Architect ratification (contract-changing, NOT folded — see `## Surfaced for review`): (1) unify trigger callbacks behind one `ctx` object [highest-leverage, BREAKING]; (2) `producer_kind(start=fn)` sugar + public `ProducerFactory` alias; (3) `b.instrument()` builder helper; (4) promote `Responder` to `substrate.api`. Reviewer rated extension points: View/Termination/Topology/Scoring drop-in; FiringPolicy closed (intentional); Instrument was surgery (helper proposed). Artifact: **232 passed + 1 skipped; ruff + mypy --strict (47) + lint-imports KEPT + conformance 15/0/1/1 — green**. NEXT (final dimension): docs/writing (White & Orwell).

- **Test-quality review #22 (dedicated DIMENSION) + fold (2026-06-14, Phase 2)** — Dedicated test-quality audit via MUTATION testing (the reviewer mutated 7 load-bearing lines, ran the suite, reverted each). Result: **6/7 CAUGHT** — every security/correctness guard is genuinely tested (the blob hex guard, `_next_round` round-robin wrap, cancel-on-judge-completion, depth-budget `<=`, refine bound, `_failure_summary` count). ONE survivor (T5, MED): `score_grades→0.0` stayed GREEN because the grader/score tests checked the loss's RANGE `[0,1]` and the output SUBSTRING `"mean="`, not the VALUE — the calibration payoff was plumbing-tested, not value-tested. FOLDED: value-assert the loss (independently recompute Brier per claim → `pytest.approx`; assert ≥1 nonzero mean) — PROVEN to now catch it (mutating `score_grades→0.0` makes both tests FAIL; reverted → pass). Runner-up (Lens 5, answering #19's open question): stale `__pycache__` caused PHANTOM failures TWICE this audit (nearly two false bug reports incl. a spurious HIGH) — root cause editable-install + cross-session stale `.pyc` shadowing edited source. FOLDED: root `conftest.py` clears the package's `__pycache__` at session start + `sys.dont_write_bytecode=True`. Reviewer confirmed the DeterministicResponder tests exercise REAL wiring (not over-mocked) and the properness test is genuine. Artifact: **232 passed + 1 skipped; ruff + mypy --strict (47) + lint-imports KEPT + conformance 15/0/1/1 — green**. NEXT dimension: API-design/extensibility.

- **Performance review #21 (dedicated DIMENSION) + fold (2026-06-14, Phase 2)** — Dedicated perf/scalability audit, MEASURED. Verdict: WITHIN BOUNDS with headroom — 55.3K appends/sec (40K floor, ~38% margin; honest, non-flaky gate, <0.5% run-to-run); the pure-Python RFC-8785 JCS encoder is the measured ~93% of the append hot loop, but append rate does NOT bind the LLM-latency workload (the A2 rationale holds), so no urgent speed fix. O(N²) trigger fan-out and O(history) `value()` scans are real cost MODELS but INVISIBLE at bundled scale (N≤50, turns≤150; per-turn cost ~0.8ms, dominated by producer-spawn not predicate fan-out). Cleared: backpressure correct (credit-gated inbox; control bypasses; no deadlock), `read_record` streams. FOLDED the two non-speed findings: (1) documented KindBuffer/BufferView unbounded growth + O(history) `value()` (parity with PerKey's N-MEM-1 — was under-documented vs PerKey); (2) `replay()` docstring now notes it MATERIALIZES the whole record (size caveat; `read_record` streams). api.md regenerated. Speed levers recorded as a non-urgent ceiling-raise (fuse the type-whitelist `_check` into the encoder pass ~20%; memoize repeated canonical bytes; native JCS). Artifact: **232 passed + 1 skipped; ruff + mypy --strict (47) + lint-imports KEPT + conformance 15/0/1/1 — green**. NEXT dimension: test-quality (mutation pass).

- **Security review #20 (dedicated DIMENSION) + fold — HIGH path-traversal vuln fixed (2026-06-14, Phase 2)** — First dedicated security audit. Found one REAL **HIGH-severity** vuln (the prime suspect): a crafted/corrupt RECORD's `$blob` field path-traverses to **ARBITRARY FILE READ** via `substrate replay --level 2` — `replay.py` re-derived a filesystem path from the record's `$blob` hex with NO validation (and `BlobStore._path_for` had the same latent bug); CRC is integrity-not-authenticity, so a malicious record passes `read_record` intact. Reviewer confirmed with a PoC (read a file outside the record root). This broke the core trust boundary — a record is meant to be inert, shareable DATA. FIXED: `is_blob_hex()` validates the digest as `^[0-9a-f]{64}$` at the choke point (`blobstore._path_for`, covering get/put) AND in replay's resolver (→ `_UNRESOLVABLE` on invalid), + a within-root `is_relative_to` backstop. Test: `test_security_blob_traversal.py` (3, incl. the exact PoC). MED folds: `score` catches a malformed/crafted Grade payload → clean `[config]` error (was an uncaught `float()` traceback); `run`/`resume --topology-module --help` now states the code-exec posture. Reviewer CLEARED (tried, held): reserved-namespace spoof (triple-enforced — a Producer cannot forge a `substrate.*` event), corrupt-frame drop-safe, NO record→code-exec path, no SSRF. Dual contract: artifact — **232 passed + 1 skipped; ruff + mypy --strict (47) + lint-imports (F-API-6 KEPT) + conformance 15/0/1/1 — green**. NEXT dimension: performance/scalability.

- **Usability review #19 (dedicated DIMENSION) + fold (2026-06-14, Phase 2)** — First dedicated usability review (3 lenses: DX-authoring, CLI-UX, conceptual model). Verdict: the surface is LEARNABLE (the reviewer authored a working topology first try from docs alone; `substrate score` closed the #18 loop), but one HEADLINE defect: `status=finalised` ≠ "it worked" — the 3 likeliest newcomer mistakes (`factory=fn` not `lambda: fn`; wrong `input_builder` arg order; predicate without `.value()`) all yield a SILENT green run (failures honestly on the record but never surfaced to the author). FOLDED: **DX-1** — `run` / `demo run` now print `[finalised] <root>  WARNING: N ProducerFailed/InputBuildFailed/PredicateQuarantined (read the record)` whenever any failure occurred, and `run --strict` exits nonzero (`cli.py:_failure_summary`); a broken run now LOOKS broken. **CLI-1** — `inspect` catches `ProducerNotFound`/`SequenceOutOfRange` → clean `[config]` error, not a raw traceback (the #16 `--producer` ref-vs-kind confusion). **Tutorial** — added the `finalised ≠ worked` callout, the callback-shape distinction (`predicate(event, views)` vs `input_builder(views, staged, event)`), §2.5 accumulate-and-gate (Views+Predicates — the #1 conceptual cliff), §2.6 Route + `staged` (the 8th primitive, previously absent: 7/8 → 8/8 covered). Tests: `test_cli_failure_surfacing.py` (4). Dual contract: artifact — **229 passed + 1 skipped; ruff + mypy --strict (47 files) + lint-imports (F-API-6 KEPT) + `substrate conformance --no-perf` 15/0/1/1 — green**. (Also reverted a premature `textual` core dep — the UI/TUI is PAUSED pending the Architect's own design.) NEXT review dimension: security, then performance/scalability, test-quality, API-design, docs.

- **Wave 13/14 follow-through — adversarial_pair + scoring payoff surfaced + catalogue + reviews #16–18 folded (2026-06-14, Phase 2)** — `adversarial_pair` topology (writer vs vulnerability-finder; Route-staged challenge; bounded refinement loop via attempt-count predicate; registered + record committed). **`substrate score <record>`** surfaces the calibration PAYOFF (per-speaker Brier/log-loss/spherical loss) — closes the cheap-talk loop (review #18 follow-through: scoring was test-only; now a user sees "S1 mean=0.20 vs S2 mean=0.56"). **`substrate topology list`** + **`substrate demo run/replay <name>`** (review #17/#18 HAT B on-ramp; F-API-6 KEPT via dynamic import + ctx.invoke, verified). **`docs/application-catalogue.md`** (sprint-100) — 9 built topologies + a candidate map, organized by the emergence-vs-faking axis (replacing the marketing "shock-and-awe vs LangGraph" axis review #14 flagged). README pointers fixed (count drift → `topology list`). Reviews **#16** (F1 correction — `all_completed`/`ended_total`; reviewer retracted its own #15 F1), **#17** (clean bill; ablation made runnable both arms, repair discriminates), **#18** (clean bill; score surfaced) all folded — each a clean bill, zero correctness defects, only honesty/UX follow-throughs. **9 bundled topologies** runnable via the CLI. Dual contract: artifact — **225 passed + 1 skipped; ruff + mypy --strict (47 files) + lint-imports (F-API-6 KEPT) + `substrate conformance --no-perf` 15/0/1/1 — green/unchanged**. NEXT: Wave 12 TUI (Textual + the UI observation contract).

- **Wave 13 (reshaped from the precursor extraction) — conversation substrate + scoring instrument + demos + emergence ablation + bundled registry (2026-06-14, Phase 2)** — Re-shaped per `docs/precursor-application-ideas.md` (ideas lifted from the `recursive_strategy_refinment` precursor, which ran these apps by HAND-ROLLING the orchestration substrate now provides natively — its cascade IS Trigger-on-chunk, its converge→cancel-peers IS cancel_all_others). Built, all on public `substrate.api`: **`conversation.py`** — the general N-speaker turn-based substrate (round-robin `Turn` cascade via one Trigger per speaker, transcript View, `Converged` early-stop via threshold_count, round budget) with optional emergence INSTRUMENTS (common-ground + repair side Producers fired per Turn and Routed back into the next speaker — the emergence wiring). **`instruments/scoring.py`** — proper scoring rules (Brier/log-loss/spherical), properness numerically tested (the property that closes the cheap-talk loop). **`instruments/common_ground.py` + `repair.py`** — typed-event side Producers (non-load-bearing). **`conversation_demos.py`** — debate (positional asymmetry), prisoners_dilemma (payoff), intel_asymmetry (information), natural_conversation (the WITH/WITHOUT instrument ABLATION — the flagship composition demo; the delta in event kinds + turn text IS the demonstration). **`bundled.py` + cli hook** — `substrate run --topology <name>` now works for all 7 bundled topologies (review #16 HAT B: was an empty registry / dead end); the CLI imports `bundled` DYNAMICALLY so F-API-6 (cli → api only) stays KEPT; `topologies/README.md` adds discoverability (run + record paths). **Review #16 HAT A folded:** corrected the F1 shared-wrong-model (see ## Surfaced) — `code_review` termination comment + the liveness-test rationale now state the true semantics (all_completed finalises failures via ended_total; watchdog is a defensive zero-Producer backstop; the only real residual is a wedged-running-forever Producer, bounded by the model timeout). Dual contract: artifact — **full suite 212 passed + 1 skipped; ruff + mypy --strict (45 files) + lint-imports (F-API-6 KEPT) + `substrate conformance --no-perf` 15/0/1/1 — all green/unchanged**. Determinism (`first_divergence` None) holds for the conversation substrate AND BOTH ablation arms (the instruments survive the added concurrency). NEXT: grader Producer (emits Grade events scored by the rules), commit Wave-13 CI records, reviewer pass (code + real-user). Closed clean.

- **Wave 11 pressure + E2E hardening + F1 watchdog correction (2026-06-13, Phase 2)** — `tests/test_topology_pressure.py` (18 tests, full suite now 182): **E2E** via `CliRunner` over the committed records (`substrate replay --level 2`, `tail` — real exit codes); **determinism** (`first_divergence` is None across two independent runs of all three topologies — D-8 log-equivalence holds under concurrency); **liveness** (the real F1); boundary quorums (1, N), single/many-chunk pair_coding, depth-1 + high-fanout (4×3 = 84-node) recursion, runaway-at-shallowest-depth, a failing-model reviewer. **Pressure testing corrected a wrong mental model (mine AND review #15's phrasing):** I'd added `quiescence_with_watchdog` for F1 believing the watchdog was a hard deadline; `policies.py:121` shows it finalises only on `quiescent and running==0`, and `seconds` is the idle-poll window. So the watchdog DOES fix the FAILURE path (a `ProducerFailed` is an end that is NOT a completion, so `all_completed` — needing `completed>=started` — can never fire and would HANG; `quiescence_with_watchdog` finalises on `running==0` regardless of how each Producer ended), but a Producer stuck RUNNING forever keeps `running!=0` and is NOT force-finalised by v1.0 — liveness there rests on the model adapter timeout (`OllamaResponder` 120s). The code comment + the liveness test were corrected to state this honestly (the first version wrongly simulated stuck-forever and hung the test — the pressure test caught it). `code_review` watchdog window is now a param. Artifact: **full suite 182 passed + 1 skipped; ruff + mypy --strict (38 files) + lint-imports + `substrate conformance --no-perf` 15/0/1/1 — all green/unchanged**. Review #16 dispatched (code review + a real-user acceptance pass — the reviewer now also uses substrate cold, as a newcomer, and reports the first-run UX). Closed clean.

- **S-11.INT — Wave 11 close + review #15 fold (2026-06-13, Phase 2)** — Committed CI-mode records for all three topologies under `src/substrate/topologies/<name>/records/ci_mode.record/` (`scripts/gen_topology_records.py`, deterministic, no network) + a Level-2 replay test per topology (`tests/test_topology_replay.py`: fresh run → `assert_replayable(record, "2")`, every recorded `TriggerFired` resolved-input re-hashes to its `input_sha256`, D-5; `replay --level 3b` asserted to raise NotImplementedError — never faked). `substrate replay <committed record> --level 2` verified by hand: 26 frames, 6 decisions verified. **Independent review #15 = PASS** (reviewer ran its own `Runtime→read_record` harness on all three and confirmed the cancellation/injection/budget facets genuinely fire on the records, the vocabulary call is right, both deviations honestly surfaced). **Folded review #15's one finding (F1, MID — a liveness gap I missed):** `code_review` had dropped the `quiescence_with_watchdog` backstop its card specifies, so an unreachable quorum (enough reviewers failing before they emit → live count < quorum → judge never fires) could hang the run; restored `any_of(cancel_all_others, all_completed, quiescence_with_watchdog(seconds=60))` (a TerminationPolicy timer, NOT a Trigger WallClock cooldown — verified the record's `replay_ceiling` stays `3a`). **Reconciled `PHASE2_PLAN.md` to the de-landmined cards (review #14 carryover C1):** S-11.INT/S-13.INT `Level 3(b)`/`byte-identical` → Level 2 (3(b) noted as the deferred A1.1 non-gate); `rostrum *` command surface → `substrate *` (the shipped entrypoint; resolves the unratified-rename trap by reverting to the real CLI, B-Q-1 still open). Dual contract: artifact — **full suite 164 passed + 1 skipped; ruff + mypy --strict (38 files) + lint-imports (F-API-6 KEPT) green; `substrate conformance --no-perf` = 15/0/1/1 unchanged**. Wave 11 closes clean; the only open items are the two surfaced dispositions awaiting Architect ratification (pair_coding gate deviation; DepthBudgetExceeded as an application kind). NEXT: Wave 13 topologies (same pattern) + Wave 10 docs (catalogue, TUI design) + Wave 12 TUI.

- **Wave 11 — priority topologies (code_review, pair_coding, recursive_decomposition), CI mode (2026-06-13, Phase 2)** — The three user-named topologies, built ONLY on public `substrate.api` (the same surface a third-party author has — F-API-6 dogfooding), each an r1/r2/r3-pattern factory using the `DeterministicResponder` (CI) / `OllamaResponder` (walkthrough) seam. Files: `src/substrate/topologies/{__init__,code_review,pair_coding,recursive_decomposition}.py` + `tests/test_{code_review,pair_coding,recursive_decomposition}_topology.py`. **code_review** = R-1 ensemble with role-distinct reviewers (security/performance/style/correctness/clarity) → a Bus-view quorum predicate fires a judge (Once) emitting `VerdictRendered` → cancel-all-others on the judge's completion (demonstrated on its OWN record via lingering reviewers — 2 real `ProducerCancelled`) → all_completed. **pair_coding** = driver streams `CodeChunk`+`ChunkBoundary` in chunked instantiations; navigator (fired per `CodeChunk`, never its own `Suggestion` — no self-feedback) reviews the accumulating-code View and emits `Suggestion`; a Route stages it (`substrate.InjectionApplied`) into the driver's NEXT instantiation's recorded resolved input. **recursive_decomposition** = planner → ONE PerEvent Trigger spawns solvers at any depth (recursive-Trigger property, D-17); a complementary depth-budget Trigger fires a guard emitting `DepthBudgetExceeded` for any subtask beyond `max_depth`, bounding even a runaway solver ON THE LOG. Sprint cards 130/131/132 de-landmined first: the `replay --level 3b` acceptance lines → `--level 2` (3(b) byte-identity is the deferred A1.1 item, NOT a gate — `replay(level="3b")` raises NotImplementedError and conformance check 6 FAILs if it does not defer), and `rostrum tail` → `substrate tail` (the shipped entrypoint; `rostrum` is nowhere in src — B-Q-1 unresolved). Topology event kinds are Producer-declared + strict-validated at the bus boundary (same convention as R-1/R-2/R-3's `Candidate`/`Verdict`/`Row`/`CodeChunk`) — NOT a `signals/0.2.json` change. Dual contract: signal — each topology's record asserted structurally (quorum-then-verdict + 2 cancellations; chunk count + suggestion-reaches-next-chunk via InjectionApplied; one-trigger-all-depths + depth distribution + runaway-bounded); artifact — **10 new tests green; full suite 161 passed + 1 skipped; ruff + mypy --strict (topology pkg) + lint-imports (F-API-6 KEPT) green; `substrate conformance --no-perf` = 15 PASS / 0 FAIL / 1 DEFERRED (check 6, A1.1) / 1 SKIPPED — UNCHANGED**. Rubber Duck Pass + 2 surfaced dispositions in `## Surfaced for review`. NEXT (S-11 tail): committed CI records under `topologies/<name>/records/` + Level-2 replay tests against them + real-Ollama walkthroughs + `docs/walkthroughs/<name>.md`; then S-11.INT. Closed clean.

- **Guided demo — `demo.sh` + `docs/demo.md` (2026-06-13, post-1.0, Architect-requested)** — A legible, runnable walk through the runtime from the committed reference records. `demo.sh` (runs end-to-end, exit 0, no LLM / no network) and `docs/demo.md` (the same walk with the real captured output annotated line by line, every claim citing seq numbers). Covers: **R-1** ensemble/adjudicator (the adjudicator Trigger fires at seq=14 carrying exactly the three candidates then on the log, while members m3/m4 are still running — concurrency AND the "enough-answers-in" predicate both visible on the log; 6 decisions verified by hash on replay); **R-2** retry/escalate/pause (InputBuildFailed on a malformed row, invalid-emission quarantine, failure-context Route → successful retry on attempt 2, a second row exhausting retries → RetryExhausted → pause-await-input → OperatorOverride → recovery → finalise; 8 decisions verified); **R-3 inner** streaming-synth-with-concurrent-checker (the on-declaration Trigger fires per complete declaration while the writer keeps streaming; 6 decisions verified); **provenance** (`inspect --ancestry` walking the artifact's causal chain artifact→typecheck→ast→writer→RunStarted); and the **conformance gate**. Linked first under README → Docs ("See it run"). Dual contract: signal — the three records' sequences narrated in the Rubber Duck Pass below; artifact — `demo.sh` exits 0, `docs/demo.md` embeds the verbatim output, all reproduced byte-identically from the committed records. The N-PERF-1 hardware-portability finding is written into the demo as-is, not hidden (see ## Drift watchlist). Closed clean.

- **Re-root to `substrate/` + private GitHub + real CI (2026-06-13, post-1.0, Architect-directed)** — Packaged the project as a standalone repo and gave the claims a CI backstop. Re-rooted the git repo at `substrate/` via subtree split (history preserved + de-prefixed, 31 commits), added `https://github.com/laffeyp/Substrate` as `origin` on `main` **after confirming the repo is private**, force-pushed. Only the `substrate/` tree is tracked (sdd-kit-2/, _factory/, the landscape notes stay out of the repo). `.gitignore` authored for the new root; the pre-re-root full-project history retained locally in `.git.preroot-bak` (see ## Deferred for the cleanup note). **Real CI** (`.github/workflows/ci.yml`): matrix ubuntu+macOS × py3.12/3.13/3.14, each job running `ruff check` + `ruff format --check` + `mypy --strict` + `pytest` + `lint-imports` (import-linter, enforcing the F-API-6 cli-imports-only-`api` contract) + `substrate conformance --no-perf`. **CI earned its keep on the first run:** mypy failed on all six jobs — `reference/_models.py` imports `httpx`, which was declared only in the `[openai-compat]` extra, never in `[dev]`; it had passed locally only because the dev venv happened to carry httpx. Added `httpx` to `[dev]`, re-locked, green 6/6. Then bumped `actions/checkout@v4→v5` and `astral-sh/setup-uv@v5→v6` to clear the Node-20-runtime deprecation (CI re-verified green — both new majors exist and work). Dual contract: artifact — CI green across all six matrix jobs on the current HEAD; repo confirmed private, default branch `main`; the cli.py "F-API-6 enforced by import-linter in CI" claim is now literally true (1 contract kept / 0 broken + the AST backstop test). Closed clean.

- **"Does it do what it says it does" audit close (2026-06-13, post-1.0, external 24-agent audit, reviews #8–#13)** — Closed the gap between an honest kernel and an over-claiming presentation layer. The audit judged the substance sound (the kernel was the real thing) but found several presentation-layer overstatements; all closed: **(a)** R-3's reference checker was a fake stand-in *named* like a type-checker — replaced with a real load-bearing `ast.parse` (it genuinely parses each declaration; the docstring qualifies it as a deterministic stand-in to swap for a real type/test checker in a user topology); **(b)** "recorded runs" were referenced but uncommitted — committed deterministic CI-mode run records (r1/r2/r3/r3-inner) that replay byte-identically, with `docs/walkthroughs/records/README.md` labeling them CI-mode/deterministic (stand-in Producers, no LLM), distinct from the real-Ollama transcripts (review #13's one remaining hygiene note); **(c)** overclaiming language ("proves") softened to "checks"/"exercises" across docs + the CLI; **(d)** the `Decision.LET_FINISH` value + `let_finish` recipe were a dead no-op path — REMOVED (re-visit condition in ## Deferred); **(e)** the rich CLI consumed `[config]`/`[lock]`/`[FAIL]`-style status text as markup tags — fixed with `Console(stderr=True, markup=False)`; **(f)** the import-linter / CI claims were aspirational at audit time — made literally true by the re-root/CI sprint above. Post-close verdict: ship. Dual contract: signal — the reference records' sequences (narrated in the Rubber Duck Pass below); artifact — **151 tests + 1 opt-in perf skip pass; `substrate conformance --no-perf` = 15 PASS / 0 FAIL / 1 DEFERRED / 1 SKIPPED; the committed records replay and hash-verify with no Ollama**. Closed clean.

- **Persistent pause/resume (F-TERM-3 / F-PERS-2) + review-#7 demo rebuilds (2026-06-13, Architect ruling — the agreed last v1.0 feature)** — The resume re-entry that was the one base-spec MUST gap. **Library:** `Runtime.resume(topology, *, resume_event)` (`runtime.py`) — refactored `run()` into `run()`/`resume()`/`_drive()`; resume requires `persistent=True` (config-time `RegistrationError`), re-acquires the flock, recovers the torn tail (`recover_open_segment`), reopens the EXISTING hot segment for append (`RecordWriter(resume=True)` — continues the same segment/seq, rebuilds the sealed list; `record.py` `_segment_index`), folds the registered Views Level-1 over the record + restores `next_seq` from the log tail + restores the original `run_id` + the started/ended counts (`_resume_bootstrap`), then injects the external resume event as a producer=null bus event (reserved-kind refused) so the resume Trigger fires the continuation → drives to terminal. Seq continues unbroken across the pause boundary; the manifest updates. **CLI:** `substrate resume <root> --topology-module M:func --input M:event_factory` (`cli.py`) replaces the exit-64 stub (shared `_load_attr` + `_drive_maybe_tailing`; exit 0/1/2/64/65/130). **`any_of` fix:** now propagates a composed member's `resume_condition` + tightest `watchdog_seconds` (`policies.py`) — a pause decided through the wrapper records its typed resume_condition. **N-MEM-1 documented:** `PerKey._seen` grows O(distinct keys) unbounded for the lifetime of a run (the cost of fire-once-per-key-forever); not bounded in v1.0 because LRU/window eviction silently lets an evicted key re-fire (a dedup-correctness change needing a decision) — operational guidance + the `staged`-is-bounded clarification in the `PerKey` docstring (`triggers.py`). **F-SCHEMA-2 EXERCISED** (`test_resume.py`): two runs at schema_version 1 and 2 under one persistent-bus root, each decoded via its OWN RunStarted manifest (`E@1`/`E@2`), no cross-run reinterpretation — honest about the one-run-per-record-root granularity. **Review #7 (coordinator-relayed, accepted as engineering guidance — NOT user authority): the reference DEMOS now demonstrate the mechanisms they name.** **R-2 rebuilt** (`reference/r2_pipeline.py`) as the real integrated cascade on a PERSISTENT bus — a REAL fault (undeclared-kind emission → `ProducerEmittedInvalidEvent`, no fabricated event reaches the bus); retry enriched via a Route carrying the failure reason (check-1 pattern) → recovers on attempt 2; an unrecoverable fault exhausts a per-row retry budget (`_InvalidByRow` View) → `RetryExhausted`; a malformed row's input_builder raises → `InputBuildFailed`; `RetryExhausted` → `pause_await_input` → `Runtime.resume` continuation. **R-1 fixed** (`reference/r1_ensemble.py`, by sub-agent) — lingering members (`slow_members`/`linger_seconds`) so cancel-all-others fires on R-1's OWN log; test renamed `test_r1_cancels_lingering_losers_on_adjudication` asserts 2 real `ProducerCancelled`. **Walkthroughs RE-RUN for real against Ollama + documented** (`docs/walkthroughs/README.md`): R-1 — weak models genuinely DISAGREE (Charisma/Vision/Integrity, 3 of 3) + 2 live cancellations; R-2 — full cascade + pause→resume end to end (continuous seq across the boundary). Tests: pause→resume in a FRESH Runtime + continuous-seq + run_id-preserved + reserved-kind-refused + CLI two-invocation resume (`test_resume.py`, `test_cli.py`). Dual contract: artifact — **148 tests + 2 perf, four gates green, `substrate conformance` = 16 PASS / 0 FAIL / 1 DEFERRED / 0 SKIPPED (perf 56,291 ≥ 40K), mypy --strict clean (34 files)**. F-TERM-3/F-PERS-2 CLEARED. Closed clean.

- **Docs N-DOC-1 (2026-06-13, Wave 9)** — The v1.0 docs. Files: `docs/tutorial.md` (first-topology tutorial — zero to a working counter→doubler two-Producer topology, design §3 shape, runnable, + reading the record / inspect / replay), `docs/api.md` (the full public surface — GENERATED from the live `substrate.api.__all__` + docstrings by `scripts/gen_api_docs.py`, which asserts the grouping partitions `__all__` so the ref can't silently drift or omit a symbol; 72 symbols / 13 groups), `docs/walkthroughs/README.md` (already present — R-1/R-2/R-3 with the REAL Ollama runs), README updated (vocab→0.2 + amendments; a "What replay means" section). **Byte-identical-replay claim QUALIFIED everywhere (carry-forward a):** README + tutorial state plainly that Levels 1/2/3a + D-8 log-equivalence ship in v1.0 and **Level-3(b) byte-identity is post-1.0** — no unqualified "byte-identical replay" claim anywhere; `--level 3b` surfaces the deferral, never fakes success. Dual contract: artifact — **144 tests, four gates green (ruff now also lints `scripts/`), mypy --strict clean (34 files); docs/api.md regenerates byte-stable from the committed script**. Closed clean.

- **Export map single-source reconciliation (2026-06-13, Wave 9 reconciliation 1)** — Removed the b.export two-source-of-truth. The composition export map is now declared ONCE, on `embedded_substrate(exports=...)` (F-COMP-1 / §20: the ExportMap is part of the EmbeddedSubstrate kind's declaration). `embedded_substrate` attaches `__substrate_export_map__` to the `start` callable; `producer_kind` builds the factory once (cheap closure construction, no inner run; guarded) to read it into `ProducerKindReg.export_map`; the RunStarted manifest DERIVES both the per-kind `producer_kinds[].export_map` (authoritative) and the topology-level `exports` aggregate from that single source — so the recorded boundary can NEVER drift from the map the translator uses. `b.export` + `Registration.exports` removed; `r3_codesynth.py` drops its redundant `b.export` call; the test now asserts the manifest derives from the embedded substrate's own map (no b.export). Non-embedded topologies: empty `exports`, no `export_map` key (verified). Files: `composition.py`, `topology.py`, `runtime.py`, `reference/r3_codesynth.py`, `test_composition.py`. Dual contract: signal — the export boundary still observable in RunStarted (check 7); artifact — **144 tests, four gates green, mypy --strict clean (34 files)**. Self-reviewed (single source confirmed, drift impossible, edge cases clean). Closed clean.

- **Wave 9 — R-1/R-2/R-3 WALKTHROUGH mode, run FOR REAL against local Ollama (2026-06-13)** — The §8 dual-mode requirement's second half: real local LLMs, not stubs. Files: `reference/walkthrough.py` (`python -m substrate.reference.walkthrough <r1|r2|r3> <root>` — runs each topology with `OllamaResponder` against localhost:11434; LOCAL models only — `llama3.2:1b` weak members + R-2 transform, `huihui_ai/qwen2.5-coder-abliterate:7b` R-1 adjudicator + R-3 writer; cloud models avoided; small inputs), `docs/walkthroughs/README.md` (exact commands + the ACTUAL recorded outputs + what each demonstrates). **Real runs verified on the dev machine:** R-1 — 4 real llama members answered "4", the real qwen adjudicator judged + chose m0 (real adjudication); R-2 — real llama transform, the seeded fault row flagged by the validator, AND row 0 showed an authentic small-model misread (`TRANSFORM`) faithfully recorded (the walkthrough surfacing real-model behavior CI's stub hides); R-3 — real qwen-coder wrote add()+mul(), chunked to 3 pieces so add() SPANS chunks (overlap), the inner pipeline detected exactly 2 declarations, composed through the embedded substrate, ONLY `OuterArtifact` crossed (no inner-kind leak), finalised. NOT faked, NOT CI-masquerade (spec §8). CI-mode tests still gate every commit; the walkthrough is a release-time documented step. Gates: **144 tests, four gates green, mypy --strict clean (34 files)**. NEXT: docs N-DOC-1 (tutorial + API ref, byte-identical-replay qualified); b.export + P-COMPOSITION reconciliations. Closed clean.

- **Wave 9 — reference topologies R-1/R-2/R-3 (CI mode) + model adapter (2026-06-13)** — The §8 acceptance topologies, dual-mode (CI gated here; the real-LLM walkthrough is a separate documented step). Files: `reference/_models.py` (the `Responder` seam — `DeterministicResponder` for CI + `OllamaResponder` over the openai-compat httpx endpoint, lazy httpx import, verified against the live Ollama at localhost:11434 with llama3.2:1b + qwen2.5-coder-abliterate:7b), `reference/r1_ensemble.py` (N candidates → Bus-view `KindBuffer` predicate "≥quorum" → Once adjudicator → cancel-all-others + all-completed), `reference/r2_pipeline.py` (parser→transform→validator PerEvent chain with a seeded fault row → validator rejection), `reference/r3_codesynth.py` (writer streams chunks → buffer-View chunk-boundary/overlap predicate fires AST once per complete declaration via a stateful PerKey count → typecheck → ArtifactReady, the whole inner pipeline wrapped as an EMBEDDED SUBSTRATE exporting only ArtifactReady onto the outer run); `views.py` `KindBuffer` (kind-subscribed buffer, F-VIEW-2) + api export; tests `test_reference.py` (CI-mode, gated). **CI mode proves WIRING, never masquerades as the demonstration** (spec §8). Bugs caught + fixed in-build: R-3 inner topology missing `b.initial("writer")` (the run hung — no initial Producer + all_completed never fires); R-3 `starts_key` typo. R-1 cancel-others demonstrated in walkthrough (instant CI candidates leave nothing to cancel; the wiring is unit-tested in test_cancel_others.py). Dual contract: signal — each reference topology's record asserted (adjudication after quorum, faulted-row flagged, only-ArtifactReady-crosses + 2 decls despite overlap + export map in manifest); artifact — **144 tests, four gates green, mypy --strict clean (32 files)**. NEXT: run the R-1/R-2/R-3 walkthroughs FOR REAL against Ollama + document them; docs N-DOC-1; b.export + P-COMPOSITION reconciliations. Closed clean.

- **N-PERF-1 re-baseline (A2) + crc-splice fail-loud guard (2026-06-13, Architect ruling + review #6)** — Architect re-baselined N-PERF-1 (the 100K floor was derived from a prototype that never measured the required canonical encoding): cut `product_spec/draft7_amendment_A2_nperf1.md` — **floor 100K → 40K appends/sec** (~28% margin under the measured ~56K), recorded that the per-append cost is the pure-Python rfc8785 encoder and a compiled JCS encoder is the post-1.0 lever (rfc8785 LEFT AS-IS — byte-identity contract). `conformance.py` check-15 floor → 40K (PASSES honestly at ~56K); `test_perf.py` floor → 40K + xfail REMOVED (it passes); `WORKING_AGREEMENT.md` perf line + amendments list updated. **Review #6 fold (byte-identity guard):** `framing.py` `frame()` now FAILS LOUD if any top-level envelope key sorts before `"crc"` (the crc-splice is correct only because `crc` sorts first; a mis-spliced frame still verifies, so this guard is the only fail-loud catch for a future schema change that would silently break canonical bytes); `test_framing.py` adds the guard test + unicode/JCS-escape payload cases (non-ASCII, embedded quote/backslash/control, U+2028/U+2029) nailing payload-independence. `subtree_cancellation` confirmed a distinct unbuilt F-LIFE-2 recipe (NOT conflated with cancel_all_others) → ## Deferred. Dual contract: artifact — **all four gates green; `substrate conformance` (with perf) = 16 PASS / 0 FAIL / 1 DEFERRED (check 6) / 0 SKIPPED**. Closed clean.

*Agent appends one entry per sprint close. Append-only.*

- **Conformance harness honesty fixes (2026-06-13, external review #5 A/B/C/D)** — Four fixes to the harness, A and C honesty-critical. Files: `conformance.py`, `cli.py`, `conformance_perf.py`, `composition.py`, tests. **FIX A (honesty gap):** `_check_6` returned DEFERRED unconditionally while computing but ignoring `l2_ok` — a real Level-2 mismatch would be MASKED as deferred (the inverse silent-pass). Now check 6 FAILs on any Level-2 mismatch; DEFERRED is scoped ONLY to the 3(b) byte-identity clause (detail says "Level 2 PASSED"). Tested (patched-replay mismatch → FAIL, not DEFERRED). **FIX B:** added a distinct `Status.SKIPPED` — check 15 under `--no-perf` is SKIPPED (a run-time skip), NOT DEFERRED (spec-amended); the CLI renders four distinct labels (`SKIPPED (--no-perf; not spec-amended)` vs `DEFERRED (spec-amended A1.1)`) and the summary counts both. **FIX C:** recorded the CI requirement (CI MUST run `substrate conformance` WITHOUT `--no-perf`, + a dedicated `pytest --runxfail tests/test_perf.py` job) to BLACKBOARD ## Deferred — the default `substrate conformance` already FAILs honestly on the unmet floor. **FIX D:** the inner_root-required raise was already tested (`test_missing_inner_root_is_recorded_not_uncitable`); added the contract doc line to `embedded_substrate` (every embedded topology MUST thread inner_root). **SPEC-CONFIRM:** documented in the perf-probe that it measures at the ≤5-substantive filtered shape — which IS N-PERF-1's defined shape (F-PRED-1), so the floor is not "100K with all 50 evaluating." Dual contract: artifact — **138 tests + 1 honest perf xfail, four gates green, mypy --strict clean (28 files)**. Closed clean.

- **N-PERF-1 optimization: batch-drain + crc-splice (2026-06-13, Architect "optimize first" ruling)** — Before/after at the faithful reference shape (50 Triggers across 10 kinds, ≤5 substantive/append): **~37K → batch-drain ~37K (no gain) → crc-splice ~56K appends/sec**; still under the 100K floor → HALTED + surfaced for the Architect's re-baseline call (see ## Surfaced for review). Files: `runtime.py` (writer-loop BATCH DRAIN — one `await inbox.get()` then a `get_nowait()` loop processes the ready batch before re-awaiting; total order / per-emission credit release / step-6 / quiescence all preserved; releases credits for unprocessed emissions on a mid-batch finalise so no task hangs on the cancellation gather), `framing.py` (CRC-SPLICE — `B_disk` = `B_hash` with `"crc":"<8hex>",` spliced after the opening brace since JCS sorts `crc` first, instead of a SECOND full `rfc8785.dumps` of the envelope; halves dumps/frame), tests `test_framing.py` (byte-identity guard: spliced frame == full re-encode across envelope shapes). **Diagnosis:** the batch-drain proving NO gain disproved my earlier asyncio root-cause; profiling pinned the dominant cost as the **pure-Python `rfc8785` JCS encoder** (~75% of writer time). The crc-splice is the safe in-wave win; the residual ceiling is the per-frame canonical encode + §4.2 whitelist walk (both D-7-required), needing a faster-JCS-encoder dependency or a fused encode pass — beyond this wave, surfaced. Behavior-preserving: the count_to_three exact-sequence oracle + all 136 tests + 1 honest perf xfail green; ruff + mypy --strict clean (28 files). Closed clean (perf re-baseline decision pending the Architect).

- **cancel-others / let-finish TerminationPolicy paths (2026-06-13, Wave 9 prerequisite for R-1)** — Wired the kernel §8 / F-LIFE-2 `CANCEL_OTHERS` runtime path the reference topology R-1 needs (it was an enum value with no recipe + no runtime handling). Files: `policies.py` (`cancel_all_others(when)` → CANCEL_OTHERS; `let_finish(when)` → graceful-drain finalise), `runtime.py` (`_consult_termination` CANCEL_OTHERS branch → `_cancel_others`: cancels every live Producer EXCEPT the subject — the producer of the deciding event, read from the lifecycle payload's producer ref per P-SUBJECT-ID since the envelope producer is null for substrate.* events; the cancelled tasks emit `substrate.ProducerCancelled` drained by the still-running writer loop; the run continues to quiescence — non-terminal, idempotent so a repeat match doesn't thrash), `runstate.py` (`task_by_instance` map so the subject can be spared), `api.py` (exports). Tested: candidates cancelled + observable on the log, adjudicator spared, exactly one cancel-others (idempotent), run finalises without hanging on 30s-sleeping candidates. `subtree_cancellation` (the spawn-tree variant) remains a documented F-LIFE-2 deferral (needs the spawn-tree; R-1 doesn't need it). Dual contract: signal — cancel-others sequence on the record; artifact — **135 tests + 1 perf xfail, four gates green, mypy --strict clean (28 files)**. Closed clean.

- **Wave 9 Batch A — conformance harness + perf probe + handshake-#4 fixes (2026-06-13)** — The 17-check release gate assembled behind `substrate conformance` (product §7; F-CLI-4). Files: `conformance.py` (NEW — 17 checks, each a canonical mini-topology asserting one §7 property over the record, returning a typed three-state `CheckResult` PASS/FAIL/**DEFERRED**), `conformance_perf.py` (NEW — the N-PERF-1 reference-shape append-rate probe), `cli.py` (`substrate conformance` wired, replacing the exit-64 stub; `--no-perf`), `api.py` (re-exports `run_conformance`/report types + — handshake-#4 Fix 5 — the replay catch-surface `ReplayError`/`RecordIncompleteError`, with an explicit documented decision on which record-internal exceptions are deliberately NOT public); tests `test_conformance.py`, `test_perf.py`. **Carry-forward (b) honored:** check 6 (Level-3b) is a GENUINE THIRD state "DEFERRED (spec-amended)" — shown distinctly, NOT counted as a pass, the summary names the deferred count, and the gate exits 0 only because no check FAILED (deferred ≠ pass). **Honest perf finding (SURFACED):** check 15 measures the REAL append rate (~37K faithful ref-shape / ~44K bare appends/sec) — BELOW the 100K floor; reported as FAIL with the live number (never fudged), root-caused to the per-event asyncio round-trip, recommended batch-drain fix surfaced for an Architect decision (see ## Surfaced for review). (The probe was corrected from a harsher all-50-per-append shape that understated at ~32K to the faithful ≤5-substantive shape at ~37K — found by self-review against N-PERF-1's F-PRED-1 clause.) The perf test is xfail(strict=False) so the gap is visible, not hidden. **Folded external-handshake #4 (composition/api):** Fix 3 — `default_export` no longer translates a FAILURE RunFinalised through the success carrier (a failure RunFinalised is skipped; inner-run failure surfaces solely as the one ProducerFailed — no spurious ProducerEmittedInvalidEvent; tested); Fix 6 — `inner_root` is now REQUIRED on the embedded input (raising `InnerRootRequired` → a recorded outer ProducerFailed, not a fabricated un-citable fallback root), so run-granularity provenance is unconditionally true; Fix 5 — replay exceptions re-exported. Operator note added (inner record is the on-disk buffer under outer backpressure). Dual contract: signal — the harness runs 17 canonical topologies; artifact — **133 tests + 1 honest xfail (perf), all four gates green, mypy --strict clean (28 files)**. Closed clean (pending the perf ruling + the between-batch review).

- **Wave 8 — composition (substrate-as-Producer + export maps) (2026-06-13) + handshake Fixes 1/2** — Closes conformance check 7 (export boundary; technical §20, F-COMP-1/2/3). Files: `composition.py` (NEW — `embedded_substrate(topology, *, exports, default_export)` returns a Producer `start` callable that runs an INNER Runtime at its own record root and translates export-mapped inner kinds onto the outer bus; `EmbeddedRunFailed`), `runtime.py` (`_producer_task` threads `inner_run_id` onto `substrate.ProducerFailed`; manifest now records `topology.exports`), `topology.py` (`b.export` docstring — declarative→manifest), `api.py` (exports `embedded_substrate`/`EmbeddedRunFailed` + the public exception hierarchy); tests `test_composition.py`. The translator subscribes (via the read-only follower) to ONLY export-mapped inner kinds and yields outer-schema Structs through ordinary OUTER admission — the entire backpressure story, no second path; the inner run proceeds untouched at its own root. Inner `substrate.*` never cross unless mapped (verified); a mis-declared outer kind becomes `ProducerEmittedInvalidEvent` at the outer boundary (not a crash). Inner-run failure → ONE outer `substrate.ProducerFailed` carrying the inner run_id (taken from the inner RunResult, robust). Nested-depth smoke test (2 levels, 3 roots). **Between-wave independent review (vs §20 + F-COMP + check 7)** found 4 spec divergences, all addressed: (#1 BLOCKER) per-frame inner provenance `{inner_run_id,inner_seq}` can't ride the locked wire ProducerRef → **surfaced as `P-COMPOSITION-INNER-PROVENANCE`** (run-granularity provenance verified: inner root in the outer TriggerFired.resolved_input + inner_run_id on ProducerFailed); (#2 default export = inner RunFinalised) → implemented via `default_export` (author-named outer carrier, since outer kinds can't be substrate.*); (#3 b.export dead) → wired into the RunStarted manifest (`topology.exports`, observable per check 7); (#4 inner_run_id scraped from polled frames) → read from `RunResult.run_id`. **Folded external-handshake #3 Fixes:** Fix 1 — the attach follower now reads INCREMENTALLY (`os.lseek` to the cursor + read only the new tail; O(new) not O(filesize) per poll) and uses a single `framing.recover` `\n`-scan for both hot and sealed segments; Fix 2 — the public exception hierarchy (SubstrateError + 7 subclasses) is re-exported from `api`, and `cli.run` now catches BY TYPE (`except api.BusLockedError`) instead of string-matching class names. Low-priority handshake items (D-8 error-class regex localization-lossiness; `safe_raw` object-address in raw_payload) logged to ## Drift watchlist. Dual contract: signal — composition boundary sequences over real records; artifact — **125 tests, all four gates green, mypy --strict clean (26 files)**. Closed clean (pending the post-fix independent review before the commit lands).

- **Wave 7 — live attach + CLI + sidecars (2026-06-13) + external-review #2 Fixes A/B** — The reader surfaces (tech §13, §6.4, §3.8; design §5; F-PERS-4, F-CLI-1..6, F-API-6, conformance check 14). Files: `attach.py` (NEW — `LiveRecord`/`attach`: poll-based CRC-verifying read-only follower; opens files read-only, never locks, never signals — F-PERS-4 by construction, asserted at the syscall level), `sidecar.py` (NEW — `DiagnosticSidecar` §3.8 + `WriterStatsSidecar` §6.4 + `read_sidecar`: off-bus buffered JSONL, never sequence-numbered), `cli.py` (NEW — `substrate run/tail/inspect/replay/validate/stats/conformance/resume`, Click+Rich, imports ONLY `substrate.api`), wired into `runtime.py`/`sequencer.py` (optional sinks, fed off-cycle, `diagnostics`/`writer_stats` flags), exported from `api.py`; tests `test_attach.py`, `test_sidecar.py`, `test_cli.py`. **F-API-6 enforced** by an AST import-lint test (cli imports no non-api substrate module — the dependency-free form of PHASE1_PLAN S-0.INT). **Conformance check 14 (diagnostic invariance):** seeded on/off test asserts the bus is D-8-identical with diagnostics/writer-stats on or off (the sinks are fed off-cycle and absent-when-disabled, so the appended frame bytes are unchanged). `tail`/`run` stdout is unwrapped `click.echo` (pipeable into jq); narration goes to stderr (Rich). `conformance`/`resume` honestly exit 64 "not yet wired" (Wave 9 / later) — NOT a false green. **Folded external-review #2:** Fix A — re-dumped `signals/0.2.json` with `ensure_ascii=True` (matching 0.1.json) so the lock diff is surgical (metadata + TriggerFired only; was ~190 cosmetic lines from an ensure_ascii flip — semantically identical, zero functional impact since nothing hashes the vocab file, but the "byte-surgical" claim is load-bearing for a lock commit); serializer pinned in `0.2-rationale.md`. Fix B — D-8 now also normalizes `finalisation_payload_dropped` (the raising-callback branch embeds `repr(exc)`, the same cross-run divergence the `error` normalization closed) with a CLASS-PRESERVING sentinel `<ClassName>` (localizes a genuine error-class divergence while dropping the variable tail); applied to `error` too; enumerated in amendment A1.2; regression test (two raising-callback runs → `first_divergence` None). Dual contract: signal — attach/tail/inspect/replay/stats sequences over real records; artifact — **112 tests, all four gates green, mypy --strict clean (25 files)**. Closed clean (pending the between-wave independent review before the commit lands).

- **Level-3(b) replay deferral — spec amendment (2026-06-13, Architect Ruling 2)** — Resolve-in-spec-first (product principle 1): a code comment can't waive F-RPLY-1's MUST / conformance check 6, so cut ADDITIVE spec amendments (base drafts preserved). Files: `product_spec/draft7_amendment_A1_replay_3b.md` (normative — A1.1: Level-3(b) byte-identical substitution re-execution deferred to a later wave; F-RPLY-1 3(b) relaxed MUST→SHOULD for the v1.0 milestone; conformance **check 6 reclassified "deferred (spec-amended)"** — the harness must surface it distinct from pass AND fail; A1.2: the D-8 supplementary-metadata exclusion set enumerated — `run_id`/`instance`/`producer.{instance,parent}`/`measured_us`/`error`/`t` excluded, application payloads verbatim), `technical_spec/draft5_amendment_A1.md` (§12 t-replay-writer note, §14 D-8 enumeration, §16 `view_at` instance-form flow-back), `replay.py` (the 3b NotImplementedError now cites A1.1), `WORKING_AGREEMENT.md` (amendments listed in the spec table). The t-replay rationale: byte-identity includes the envelope `t`, so 3(b) needs a replay-mode writer that replays recorded `t` rather than re-sampling the clock — a mechanism the specs left open; the deferral keeps Levels 1/2/3a shipping. Dual contract: artifact — **90 tests, all four gates green, mypy --strict clean (22 files)**. The three Wave-6-surfaced Architect items are now all resolved (Ruling 1 = v0.2; Ruling 2 = this amendment; Ruling 3 = the view_at Drift-watchlist flow-back). Closed clean.

- **Vocabulary v0.2 bump + D-8 honesty (2026-06-13, Architect Ruling 1)** — The founding vocabulary's first evolution. Files: `signals/0.2.json` (NEW — additive successor, `prior_version:"0.1"`, `locked:true`; `signals/0.1.json` retained untouched as the v0.1 audit trail), `signals/0.2-rationale.md` (NEW), `signals/proposals.json` (three proposals → `ratified:true, ratified_into:"0.2"`), `constants.py` (`VOCAB_VERSION="0.2"`), `runtime.py` (`RunStarted.config.vocab_version` self-describes the record), `inspect.py` (D-8 exclusion set), `WORKING_AGREEMENT.md` (vocab location). **Ratified:** `P-TRIGGERFIRED-INSTANCE` (spawned Producer ULID — provenance/check 11 under strict extras), `P-TRIGGERFIRED-FACTORY` (spawned kind — kernel-vs-tech drift), `P-TRIGGERFIRED-INPUT-BLOB` (`$blob`→`input_blob`). v0.2 changes EXACTLY the metadata + the `substrate.TriggerFired` tag; all 38 entities, the other 11 tags, every Layer-3–10 section byte-identical to v0.1 (diff-verified). **Folded external-review #1 (D-8 honesty):** the D-8 comparison now excludes `measured_us` (wall-clock μs on PredicateQuarantined — two budget-quarantine runs were spuriously diverging, a false check-13 positive) and normalizes `error` reprs to a stable sentinel; documented in the v0.2 rationale + the Ruling-2 spec amendment. Also (review minor b): RecordWriter construction now PROPAGATES on failure (symmetry with the lock path — pre-run setup, not a run failure). Dual contract: artifact — **90 tests, all four gates green, mypy --strict clean (22 files); signals JSON valid**. Independent review: additive bump verified (diff = metadata + TriggerFired only), proposals ratified, the measured_us false-divergence closed by a two-run quarantine regression test (first_divergence None). Closed clean.

- **Wave 6 — replay + inspection (2026-06-13)** — The read-side reconstruction surface (tech §12, §14). Files: `inspect.py` (`explain_producer`, `trace_ancestry`, `view_at`, `decisions_between`, `first_divergence`; typed `Explanation`/`Divergence` structs), `replay.py` (`replay(record, level)` → `ReplayResult`; `assert_replayable`; `HashMismatch`); exported from `api.py`; tests `test_inspect.py` + `test_replay.py`. **explain_producer/trace_ancestry** use the `TriggerFired.instance` field to link a ProducerStarted back to its exact firing and follow `producer.parent` to the RunStarted root (acyclic, cycle-guarded); a missing link raises `ProducerNotFound` (no fabricated cause) — provenance closure, **conformance check 11**. **view_at(record, seq, view_instance)** folds matching events ≤ seq into a fresh View (Level-1 truncated; `SequenceOutOfRange` past the end) — **check 12**; takes a View instance, not a name (a record has no View code — tech §16 flow-back). **decisions_between(a,b)** filters `substrate.*` frames in range as typed Events. **first_divergence** builds the D-8 comparison sequence `(kind, decision-identity hash)` per frame, normalizing run-varying identity (run_id/instance ULIDs, producer→{kind}) ONLY on `substrate.*` lifecycle frames — application payloads compared verbatim — so two runs of the same deterministic topology are equivalent while a perturbed decision localizes — **check 13**. **replay**: Level 1 (stream + counts), Level 2 (recompute every TriggerFired `input_sha256` over the recorded resolved input — inline AND blob-offloaded re-hashed from the store; a malformed firing missing both input fields is flagged, not skipped; D-5), Level 3(a) precondition gate (refuse unless `replay_ceiling=="3a"` AND all Producer kinds author-flagged deterministic AND no Level-2 mismatch — F-RPLY-1 honest refusal). **Level 3(b) DEFERRED** (raises NotImplementedError; needs a t-replay/clock-substitution decision — see ## Deferred; not faked). Dual contract: signal — provenance/view-at/divergence/replay sequences asserted on records; artifact — **89 tests, all four gates green, mypy --strict clean (22 files)**. Independent adversarial review (grounded vs tech §12/§14, product §7 checks 6/11/12/13, D-8): two code findings fixed (D-8 strip scoped to lifecycle frames so an application field named `instance`/`run_id` is no longer silently erased; Level-2 malformed-firing now flagged). Three SURFACED-to-Architect items, not code bugs: (1) `TriggerFired.instance`/`factory` are unratified under strict validator-extras yet are the provenance foundation — needs P-TRIGGERFIRED-INSTANCE/FACTORY ratification + a v0.2 bump; (2) Level-3(b) deferral conflicts with F-RPLY-1's MUST + check 6 — needs an Architect-sanctioned spec amendment (relax F-RPLY-1 / descope check 6 for this milestone) since the supervisor directed the deferral; (3) view_at instance-vs-name signature flow-back. Closed clean (pending those Architect rulings).

- **runtime.py God-module refactor (2026-06-13)** — Behavior-preserving split of the 752-LOC Runtime God-class (Architect-mandated hard gate before resuming features). New files: `runstate.py` (87 LOC — `RunPhase` enum {RUNNING/PAUSED/FINALISED/FAILED} replacing the four ad-hoc booleans, + `RunState` dataclass holding the ~two-dozen per-run mutable fields, ending two-phase construction on the Runtime instance) and `sequencer.py` (397 LOC — `AppendCycle`, the single-writer six-step append cycle + helpers `_resolve`/`_emit`/`_maybe_offload`/`_resolved_input_fields`/`_track_lifecycle`/`_view_failure`/`_stage_routes`/`_eval_triggers`/`_quarantine` + the `_Emission`/`_Lifecycle` dataclasses + `subscribed()`, operating on a shared RunState). `runtime.py` shrinks 752→410 LOC and keeps only run lifecycle, locking, the writer loop, Producer tasks, and termination — constructing one RunState + one AppendCycle per run() and delegating each event to `self._cyc.cycle()`. `record.py` gains a `put_blob()` Law-of-Demeter passthrough (the runtime no longer reaches `self._record.blobs.put`). The single `_emit` helper builds the envelope dict AND the typed Event from one seq/t/payload (killing the parallel hand-built construction + drift risk). Dual contract: signal — every prior runtime test sequence reproduced unchanged; artifact — **88 tests, all four gates green, mypy --strict clean (22 files)**. Independent review (grounded vs kernel v15 six-step order + tech §6.2/§6.3/§5.2): all 8 behavior-preservation points PRESERVED; the one deliberate change (`_consult_termination` sets FINALISED only `if phase is RUNNING`) is correct + necessary — it compensates for folding the old independent `_terminated`/`_failed` booleans into one enum and HARDENS the view-failure-mid-finalise case (a mid-cascade FAILED is no longer clobberable to finalised). No regression. Wave 6 (replay+inspect) remains held uncommitted until its own commit. Closed clean.

- **Safety net + review-pass-2 correctness (2026-06-13)** — A second external review (run after the Priority-1 commit) surfaced a real non-termination hazard and five correctness defects; fixed in priority order. Files: `pyproject.toml` (pytest-timeout dep + `timeout=30`), `runtime.py`, `policies.py`, `triggers.py`, `topology.py`; new `tests/test_liveness.py`; `tests/test_robustness.py` additions; proposal `P-TRIGGERFIRED-INPUT-BLOB`. **Priority A (safety net):** added `pytest-timeout` + a suite-wide 30s timeout so any never-quiescent writer or self-feeding-Trigger loop FAILS FAST instead of wedging the machine (an earlier non-terminating revision had spun orphaned pytest processes to load-avg 66); added `test_liveness.py` — self-feeding Triggers (a Producer emitting an event its own Trigger subscribes to, the kernel's named divergence hazard) bounded by Once / cooldown / PerKey-dedup, each asserting termination with a finite event count under an explicit per-test timeout. **Priority B (correctness):** (3) **PerKey + cooldown silent data loss** — `policy.admit()` ran BEFORE the cooldown gate, so a cooldown-suppressed cycle still consumed PerKey's seen-key, permanently losing that key's only firing; reordered the cooldown check BEFORE admit (regression test with the consume-then-reappear sequence). (4) **lock-acquire-outside-try fd leak** — moved the flock acquire so a BusLockedError/UnsupportedPlatformError still propagates to the caller (config-time refusal) while a RecordWriter-construction failure after the lock now hits the finally that releases it (no self-deadlock on same-process retry). (5) **swallowed kernel errors** — the bare `except Exception` now binds the exception, appends `substrate.RunFinalised{reason:"kernel_error", error}` to the log (best-effort, never after close/fsync-fail), and yields status=failed — a kernel bug is no longer a silent truncated record. (6) **dead finalisation_payload** — implemented the real mechanism: `TerminationPolicy(finalisation=…)` produces the run's final output at finalise time, sanitized/offloaded, recorded in RunFinalised.finalisation_payload AND surfaced on RunResult; absent → None with no key (not an always-None promise). (7) **overloaded `$blob` sentinel** — the oversized TriggerFired resolved-input field renamed `$blob`→`input_blob` (carrying the BlobRef) to kill the `{"$blob":{"$blob":…}}` field-key collision; the emission-payload blob case keeps the bare BlobRef (no collision there); filed `P-TRIGGERFIRED-INPUT-BLOB`. Dual contract: signal — liveness/cooldown/finalisation/kernel-error sequences asserted on the record; artifact — **87 tests, ruff check + ruff format --check clean, mypy --strict clean (20 files; inspect/replay present in-tree but Wave 6 is committed separately after the runtime refactor gate)**. NEXT (hard gate, Architect-directed): the runtime.py God-module refactor (extract the append-cycle Sequencer, _RunState dataclass, single _emit, RunState enum, RecordWriter.put_blob) BEFORE resuming Wave 6.

- **Wave 6-prep / Priority-1 robustness hardening (2026-06-13)** — Closed the external code review against the live runtime. Files: `runtime.py`, `encoding.py`, `triggers.py`, `topology.py`, `policies.py`, `pyproject.toml`; new tests `test_robustness.py`; proposal `P-TRIGGERFIRED-INSTANCE` filed (+ `P-TRIGGERFIRED-FACTORY` disposition updated). Fixes: (1) **try/finally around the run body** — task cancellation + manifest/`close()` + lock release ALWAYS run; a writer-loop or FsyncError exception no longer leaks tasks or skips finalisation, and yields `RunResult.status="failed"`; dead `else "finalised"` branch removed. (2) **One shared sanitize-or-log helper** (`encoding.try_canonical`/`safe_raw`/`_stringify_non_canonical`) routed through all five user-value ingestion points — emission boundary `raw_payload` (now frame-safe: out-of-range ints/NaN/Inf/non-str-keys/foreign types stringified, since `msgspec.to_builtins` range-checks nothing), resolved trigger input, initial input at bootstrap, and the PerKey firing key — so a non-canonical value becomes a recorded `ProducerEmittedInvalidEvent`/`InputBuildFailed`, never an uncaught writer crash. (3) **Blob offload wired** (`BLOB_THRESHOLD_BYTES`): emissions AND `TriggerFired.resolved_input` over 16 KiB canonical bytes are write-ahead-stored and replaced with a `$blob` reference before framing (tech §3.7/§3.3, D-5 — `$blob` is a top-level field mutually exclusive with `resolved_input`; `input_sha256` still hashes the original canonical bytes). (4) **Four silent no-ops wired:** `persistent=True` acquires/releases the flock (Windows → `UnsupportedPlatformError`); trigger-level `Logical(n)` cooldown enforced in the runtime, counted in **subscription-matching** appends (kernel §6), with `WhileTrue` self-throttle removed so there is exactly one enforcement point (the builder lifts a `WhileTrue` policy cooldown to the trigger level); `quiescence_with_watchdog(seconds=)` drives `_poll_s` (bounded by the fine-grained default so a large window never delays detection); view-failure `RunFinalised{reason:view_failure}` emitted from step 3 with a `_terminated` guard in `_cycle` + `_control.clear()` so nothing follows the terminal (RUN-BOUNDARY at-most-once). (5) **ruff gates reconciled** — `extend-exclude=["kernel_spec","proof"]` + `ruff format` over the package; `ruff check` AND `ruff format --check` now BOTH exit-0. (6) `topology.view()` rejects an empty Subscription like trigger/route. Dual contract: signal — view-failure/cooldown/blob sequences asserted on the record; artifact — **59 tests, ruff check + ruff format --check clean, mypy --strict clean (18 files)**. Two independent adversarial review passes (Rubber Duck): the first found 2 blockers (double-finalise after view failure; `$blob` nested inside `resolved_input`) + majors (raw-seq cooldown count, double WhileTrue cooldown, hash-from-unsealed); all addressed; the second pass verified all five fixes HOLD with no new issues. `factory`/`instance` on `TriggerFired` carried implemented-ahead-of-ratification under the proposal taxonomy (instance is Architect-directed, closes F-OBS-2/check 11). Closed clean.

- **Waves 3–5 (2026-06-13)** — The writer + append cycle + primitives + RunStarted; the runtime is alive end-to-end. Files: `views.py`, `triggers.py`, `policies.py`, `topology.py`, `runtime.py`, `sealing.py`, `testing.py`, `api.py`; tests `test_runtime.py`, `test_routes.py`, `test_sealing.py`. Implements the v15 6-step append cycle (single writer; credit-gated inbox with control bypass; internal control-queue drain in step 6; reentrancy guard; as-of-N snapshot — no awaits in the cycle), boundary validation (§8.1 → `ProducerEmittedInvalidEvent` with the three reasons), input sealing (§8.3), the 12 lifecycle events with locked-vocab payloads, the `RunStarted` manifest (msgspec JSON-schema descriptors + fingerprints), `TerminationPolicy` + quiescence, `TopologyBuilder` + registry, and the F-API-4 record-assertion helpers. Dual contract: **signal** — `count_to_three` reproduces the design §3.2 sequence EXACTLY (`RunStarted, TriggerFired, ProducerStarted, CountReached×3, ProducerCompleted, TerminationMatched, RunFinalised`; seq 0–8 dense); **artifact** — 42 tests, ruff clean, mypy --strict clean (18 files). Conformance cores: **1/2** (cascade order via control-queue FIFO + same-cycle Route staging), **3** (bound-1 backpressure liveness), **4** (invalid-emission), plus provenance (every `ProducerStarted` carries subject identity + parent). Between-waves Rubber Duck Pass run by an independent review agent — dispositions in Sprint tail.

- **Wave 2 (2026-06-13)** — The run record on disk. Files: `src/substrate/errors.py` (typed exception hierarchy, design §6.3), `framing.py` (frame format + CRC32 + `B_hash`/`B_disk` + torn-tail `recover`, §3.3), `blobstore.py` (content-addressed write-ahead blob store, §3.7), `locking.py` (flock persistent-bus lock + Windows `UnsupportedPlatformError`, §11), `record.py` (`RecordWriter` with seal-on-roll segments + fsync policies `NoFsync`/`Interval`/`Always` + macOS `F_FULLFSYNC` durable path + the §5.2 fsync-failure path; `read_record`; writer-side `recover_open_segment`); tests `test_framing.py` (+ hypothesis round-trip property), `test_record.py`, `test_locking.py`. Dual contract: signal vacuous (runtime not built) + artifact pass — **29 tests, ruff clean, mypy --strict clean (10 source files)**. Conformance cores covered: **check 16** (torn-tail recovers by CRC scan to the last complete frame; torn bytes physically truncated, no heuristic) and **check 10** (second runtime on a locked root fails fast with advisory contents). **Resolved-here (caught by the mypy gate):** initial `_do_fsync` used non-existent `os.F_FULLFSYNC`/`os.fcntl` (guard always False → silent degrade to plain fsync, defeating §5.2 macOS durability); fixed to the real `fcntl.F_FULLFSYNC` (=51, verified present). **Spec reconciliation (resolved-here):** design §3.2 shows a finalised single-segment record still `.open` vs tech §2 "seal on finalise" — adopted seal-on-roll-only (completeness = terminal RunFinalised; recovery always runs on the `.open` segment), matching the design example; see Drift watchlist. Closed clean.

- **Wave 0 + S-1.1 (2026-06-13)** — Project skeleton + shared contracts + canonical encoding. Files: `pyproject.toml` (src-layout, py3.12+, deps pinned per tech §22), `src/substrate/__init__.py`, `README.md`, `src/substrate/constants.py` (tech §19 defaults + reserved namespace + 12 lifecycle kinds), `src/substrate/types.py` (`Event`/`ProducerRef`/`BlobRef`/`Subscription` frozen Structs, envelope per §3.4), `src/substrate/protocols.py` (`Producer`/`View` Protocols), `src/substrate/encoding.py` (JCS pipeline + §4.2 type whitelist + content hash), `tests/test_types.py`, `tests/test_encoding.py`. **Bridge mappings VERIFIED** (not guessed): msgspec 0.21.1 + rfc8785 0.1.4 introspected from the installed libs; WORKING_AGREEMENT § External SDK bridge mappings updated from STUB → VERIFIED. Dual contract: signal (vacuous — runtime not built yet) + artifact (15 tests pass; `ruff` clean; `mypy --strict` clean on 5 source files — pass). Toolchain: uv-provisioned CPython 3.12.13 (system py was 3.9 — D-6 required 3.12+). Rubber Duck Pass (no runtime trace): narrated self-review vs the locked vocabulary + tech §3.4/§4 + §19 — encoding enforces the whitelist BEFORE rfc8785 (not relying on the encoder to reject non-finite/out-of-range), bool-before-int ordering correct, content-hash `sha256:` prefix per §4.2; no anomalies. Closed clean.

- **Sprint 000 (2026-06-12)** — Vocabulary Session (founding act). Files authored: `signals/0.1.json` (locked v0.1 — 38 entities, 14 categories, 12 reserved `substrate.*` lifecycle tags, layers 3–10), `signals/0.1-rationale.md` (signed), `signals/proposals.json` (27 proposals; 3 ratified into v0.1), `signals/research-pass.md`, `kernel_spec/v16_reconciliation_note.md`. Dual contract: signal (vacuous — content sprint) + artifact (all files exist; `0.1.json`/`proposals.json` validate as JSON — pass). Produced via a 15-agent parallel workflow with adversarial verification. Architect ratified `P-SUBJECT-ID`/`P-INJECTION-FIELDS`/`P-INVALID-PRODUCER-FIELD` into v0.1 and flagged kernel v16. `spec_ambiguity` halt resolved. v0.1 LOCKED. Phase 1 (implementation) unblocked.

---

## Deferred

*Anyone may append. Re-visit conditions noted.*

- **2026-06-13 (Agent, session close) — `v1.0.0` tag PARKED at the Architect's explicit direction ("don't tag anything yet").** The code/spec axis is taggable: every base-spec functional MUST is shipped or spec-amended-with-rationale, conformance is an honest 15/0/1/1 under `--no-perf` (16/0/1 with perf on representative hardware), CI is green, the repo is private. The one non-code blocker is §12 gate (g) — external adoption, a topology built by someone outside the project before 1.0 is cut (a process item, not code). Re-visit: when the Architect says tag, or when an external topology lands. The full post-1.0 deferral list (replay 3b byte-identity A1.1; compiled JCS encoder A2; subtree_cancellation vocab-block; per-frame composition inner provenance; N-MEM-1 PerKey-set bounding; wall-clock-cooldown quiescence; the external-adoption gate) is enumerated in the FINAL §12 status in ## Surfaced for review and in CONTRIBUTING.md — all confirmed post-1.0-with-cause, none a base-spec MUST.

- **2026-06-13 (Agent, session close) — local-only cleanup: `.git.preroot-bak` (the pre-re-root full-project git history) can be deleted once the Architect is confident.** Not a project artifact, not tracked, sits outside the `substrate/` repo. The auto-mode permission classifier declined the `rm -rf` for the Agent (irreversible, outside repo scope); harmless to leave (disk only — the full history is on GitHub and in `substrate/.git`). Re-visit: the Architect runs `rm -rf .git.preroot-bak` from the project root when ready.

- **2026-06-13 (Agent, audit #8 honesty pass) — `let_finish` recipe + the `let-finish` Decision value REMOVED for v1.0; deferred post-1.0.** External "does it do what it says" audit found `let_finish()` returned a Decision the runtime's `_consult_termination` had no branch for — a DEAD path (silent no-op / possible non-finalisation). The recipe's *body* modelled "drain then finalise" by only returning FINALISE_RUN once already quiescent+drained, so it never actually emitted `Decision.LET_FINISH` — meaning the enum value was dead AND the recipe was a confusingly-named alias for "finalise on quiescence" (which `quiescence_with_watchdog`/`all_completed` already do). Rather than ship a no-op, REMOVED: the `let_finish` recipe, the `Decision.LET_FINISH` member, the `api.__all__` export, the gen_api_docs GROUPS entry (api.md regenerated → 71 symbols). **Re-visit condition:** ships when the genuine drain-then-finalise mechanism is built — a runtime dispatch branch that STOPS admitting new work (closes the admission gate) and finalises once in-flight Producers drain (distinct from quiescence, which waits for natural idle). That is real concurrency work; not rushed into v1.0. The other F-LIFE-2 recipes (`cancel_all_others`, `quiescence_with_watchdog`, `threshold_count`, `all_completed`, `pause_await_input`, `any_of`, `all_of`) ship. Recorded in CONTRIBUTING.md's deferral list.

- **2026-06-13 (Agent, confirmed for the Architect) — `subtree_cancellation` does NOT ship in v1.0; it needs a locked-vocab change (a 6th decision value or a scope field) → surfaced, not invented.** F-LIFE-2 lists `subtree_cancellation` as a distinct named recipe (cancel a Producer AND its trigger-descendants), separate from `cancel_all_others` (built; cancels all-but-subject). The blocker is NOT the spawn-tree walk (that is reconstructable from `producer.parent`) — it is that the LOCKED `substrate.TerminationMatched.decision` enum is EXACTLY the five kernel-§8 values (cancel-others, let-finish, finalise-run, pause-await-input, continue); there is no subtree-scoped cancellation decision, and the kernel itself calls subtree cancellation "an opt-in named composition in the standard policy recipe book" — i.e. it must compose through the existing decisions, but none expresses "cancel just this subtree." Shipping a faithful `subtree_cancellation` therefore needs EITHER a 6th decision value `cancel-subtree` OR a scope field on the cancel decision — a vocabulary change. NOT bumped unilaterally. **Surfaced to the Architect (see ## Surfaced for review):** is subtree_cancellation a v1.0 requirement (→ a v0.3 decision-enum proposal) or a post-1.0 recipe? `cancel_all_others`, `let_finish`, `quiescence_with_watchdog`, `threshold_count`, `all_completed`, `any_of`, `all_of`, `pause_await_input` all ship; subtree_cancellation is the one F-LIFE-2 recipe that is vocab-blocked. None of R-1/R-2/R-3 need it.

- **2026-06-13 (Agent, review #5 FIX C) — CI MUST run `substrate conformance` WITHOUT `--no-perf`.** When CI is wired (it does not exist in the repo yet), the conformance job MUST invoke `substrate conformance` with NO `--no-perf` flag, so check 15 (N-PERF-1) runs for real and a floor miss surfaces as a FAIL (exit 1) — the `--no-perf` path turns check 15 into SKIPPED and would mask the gap. Additionally, a dedicated perf job should run `pytest --runxfail tests/test_perf.py` so the perf xfail becomes a hard gate there. Re-visit at the CI-wiring sprint; until then the DEFAULT `substrate conformance` (no flag) already FAILs honestly on the unmet floor, and test_perf records the number. (Moot once the floor is met — but the gate must show the FAIL until then.)

- **2026-06-13 (Agent, external-review #2 carry-forward a) — WAVE-9 DOC GATE: qualify any "byte-identical replay" claim.** Level 3(b) byte-identical substitution re-execution IS the product's flagship "byte-identical replay" promise, and it is deferred (spec amendment A1.1). When docs land (N-DOC-1, Wave 9), any external/README/marketing "byte-identical replay" claim MUST be qualified as: "Levels 1/2/3a + D-8 log-equivalence ship in v1.0; Level-3(b) byte-identity is post-1.0." Do NOT advertise a tier that isn't shipping. Re-visit at Wave 9 docs.

- **2026-06-13 (Agent, external-review #2 carry-forward b) — WAVE-9 ACCEPTANCE: check 6 must be a genuine THIRD status.** Amendment A1.1 requires the conformance harness to surface check 6 as a real "deferred" state, distinct from pass AND fail. When the harness is built (Wave 9, `substrate conformance`), wire "deferred" as a genuine third status — NOT a pytest skip/xfail that prints green, and NOT a silent pass. A "deferred" that reads as pass is exactly the silent-pass the amendment exists to prevent. Wave-9 acceptance criterion. (The CLI's `substrate conformance` already exits 64 "not yet wired" rather than a false green, holding the line until the harness lands.)

- **2026-06-13 (Agent)** — **Replay Level 3(b) (substitution re-execution) deferred — needs a t-replay decision.** Levels 1, 2, and the 3(a) precondition gate are implemented (`replay.py`). Level 3(b) re-runs the kernel with every Producer replaced by a log-backed deterministic emitter replaying recorded emissions in recorded admission order, to a byte-identical log (tech §12, conformance check 6). It is NOT faked — `replay(level="3b")` raises NotImplementedError with the reason. The blocker: 3(b) must reproduce byte-identical frames including the `t` (wall-clock) field, but `t` is `time.time()` at append; a byte-identical re-execution needs a **t-replay / clock-substitution decision** (replay the recorded `t` values rather than re-sampling the clock) that the specs leave open. D-8 EXCLUDES `t` from equivalence, so 3(b) byte-identity and D-8 equivalence are two different bars; the only true blocker is the clock (admission order is already seq order). **RESOLVED-AS-DEFERRED 2026-06-13 (Architect Ruling 2, resolve-in-spec-first):** cut additive spec amendments — `product_spec/draft7_amendment_A1_replay_3b.md` (A1.1: F-RPLY-1 3(b) relaxed to SHOULD for the v1.0 milestone; conformance **check 6 reclassified "deferred (spec-amended)"** — the harness must surface it distinct from pass/fail, never silently failing) and `technical_spec/draft5_amendment_A1.md` (§12 t-replay note). 3(b) implemented in a later wave (build the replay-mode/t-replay writer). `replay(level="3b")` raises NotImplementedError citing A1.1.

- **2026-06-13 (Agent)** — **Wall-clock-cooldown quiescence deferred.** The quiescence check fully handles logical cooldowns (the kernel-stated vacuous case); pending wall-clock-cooldown timers are not yet folded into the quiescence input (kernel §Quiescence final clause). Re-visit when `WhileTrue` + `WallClock` firing is implemented (the runtime already owns the clock + the replay-ceiling demotion to 3b is wired). v0.1 topologies using wall-clock cooldowns are flagged `replay_ceiling=3b`.

- **2026-06-13 (Agent)** — **S-1.INT RFC 8785 conformance vectors pending fetch.** Wave 1 closes its determinism check via unit tests now, but the authoritative RFC 8785 (JCS) conformance test vectors must be FETCHED from the standard / the rfc8785 project (never fabricated) and wired into CI per tech §4.1. Re-visit at the Wave-1 integration close / first CI setup sprint. Also add a `hypothesis` canonical-encoding round-trip property test.

- **2026-06-12 (Architect direction)** — **B-Q-1 package/import name deferred.** Working name "Substrate" used throughout for now; no packages published. Re-visit before the first sprint that authors `pyproject.toml` or fixed import paths (Wave 0 of Phase 1). Until then, the canonical home registry uses placeholder import root `substrate`. Shortlist + brainstorm preserved in `## Open questions` B-Q-1. The kernel spec's *concept* word "the substrate" is unaffected regardless of the eventual package name.

---

## Open questions

*Anyone may append.*

- **B-Q-1.** Package/import name — **DEFERRED** 2026-06-12 (see `## Deferred`). Working name "Substrate" stands; official name picked later. Was: product D-2 shortlist (`substrate-kernel`, `substrate-bus`, `pysubstrate`, `substrated`, `horizon-substrate`, `buskernel`); design-spec placeholder `rostrum`; brainstorm added Strata / Keel / Chronicle / Cradle.

---

## Drift watchlist

*Agent maintains. Patterns to monitor across sprints. When the same observation surfaces in three consecutive sprints, escalate to `## Surfaced for review`.*

- **2026-06-13 (Agent, post-1.0 demo session) — N-PERF-1 is hardware-variant; the floor is not portable, which is exactly why it is out of CI.** The same append-rate probe measures **~56K appends/sec on the build machine** (passes the A2-recalibrated 40K floor) and **~26K on this laptop** (fails it). This is not a regression — it is the per-frame pure-Python rfc8785 (JCS) canonical encode (D-7-required for byte-identity) being CPU-bound and machine-dependent. Design consequence already in place: conformance check 15 runs `--no-perf` in CI (so a slow runner never red-flags the suite) and the floor gate is opt-in locally (`SUBSTRATE_PERF_GATE`) / run on controlled hardware at release; `docs/demo.md` states the laptop number and the failure plainly rather than hiding it. Watch: if anyone proposes putting the perf floor back into the CI matrix, this is why not. The post-1.0 lever if a deterministic firehose ever needs more is a compiled (C/Rust) JCS encoder — do NOT hand-write one (byte-identity contract).

- **2026-06-13 (handshake low-priority a) — D-8 error-class regex is localization-lossy for non-standard exception names.** `inspect._EXC_CLASS_RE` matches names ending Error/Exception/Warning/Interrupt; a custom exception class (`class Boom(Exception)`) or `SystemExit`/`StopIteration` falls back to the `<error>` sentinel. NOT a correctness bug (two such errors still compare equal under D-8; the relation stays sound) — only LOSSY localization (a Boom-vs-KeyError divergence reads as `<error>` vs `<error>` and won't localize). Revisit if a topology surfaces custom exception classes whose divergence must localize; broaden the regex or carry the class name explicitly then.

- **2026-06-13 (handshake low-priority b) — `safe_raw` raw_payload can embed an object address for a non-canonical OBJECT emission.** On `substrate.ProducerEmittedInvalidEvent`, a non-canonical emission of a foreign OBJECT (not a dict/list/scalar) is preserved verbatim via `safe_raw` → `repr(obj)`, which for a plain object embeds its memory address (`<X object at 0x...>`). That `raw_payload` is on a `substrate.*` frame and is NOT D-8-normalized, so two runs emitting the same bad object could false-diverge on the address. Extremely narrow (requires emitting a non-Struct, non-whitelist object and then D-8-comparing two such runs); lowest priority. Fix-if-it-bites: route `raw_payload` reprs through the same class-preserving/address-stripping normalization, or D-8-normalize `raw_payload`.

- **2026-06-13 (Agent) — composition per-frame inner provenance `{inner_run_id, inner_seq}` not stamped (§20).** The boundary translator yields bare outer Structs; the locked wire ProducerRef `{kind,instance,parent}` has no metadata slot and outer kinds can't be `substrate.*`, so per-FRAME inner provenance has nowhere to ride. Run-granularity provenance HOLDS (inner root in the outer TriggerFired.resolved_input; inner_run_id on ProducerFailed). Surfaced as `P-COMPOSITION-INNER-PROVENANCE` (proposals.json) for an Architect ruling + likely vocab bump. Watch for a reader needing to trace an outer event to an exact inner seq.

- **2026-06-13 (Agent) — ~~`b.export` vs `embedded_substrate(exports=)` two-source-of-truth~~ → RESOLVED 2026-06-13.** Was: two parallel export-map sources could drift. RESOLVED in the export single-source reconciliation (see ## Built): `b.export` + `Registration.exports` REMOVED; the export map is declared once on `embedded_substrate(exports=)` and the manifest DERIVES from it (per-kind `producer_kinds[].export_map` + the topology aggregate). One source; drift impossible.

- **`view_at` instance-vs-name signature (tech §16 flow-back).** §16 specifies `view_at(record, seq, view: str)`; implemented as `view_at(record, seq, view: View-instance)` because a record stores no View code. Documented flow-back (amendment A1.c); no code change.

- **Vocabulary-already-in-spec.** The kernel spec pre-names lifecycle kinds and the envelope. Watch that Sprint-0 layers 0–2 transcribe the spec faithfully rather than re-inventing or silently renaming (e.g., spec says `ProducerEmittedInvalidEvent`, not `InvalidEmission`).
- **Orchestration vs. kit purity.** We are layering teams + worktrees + best-of-N on top of a kit that defers these to Section 3. Watch that the orchestration never bypasses the dual contract, the Rubber Duck Pass, or the vocabulary lock (hard rule 10 — no silent hand-author).
- **Producer protocol shape (design §9.6 callable ⇆ tech §16 `.start` method).** Implementation follows design §9.6 (a Producer is a callable `(input)->AsyncIterable[Event]`, factory returns it) — reasoned and asyncio-native. Flow-back: technical §16 should show the callable form, not the object-with-`.start()` form.
- **Input-sealing accepted set (tech §8.3).** Implementation seals dict/list into immutable forms (MappingProxyType / tuple) so design §3.3's ergonomic dict inputs work while staying immutable-by-construction; §8.3 lists frozen-only. Flow-back: §8.3 should list immutable-sealed dict/list among accepted forms.
- **Seal-on-roll vs seal-on-finalise (design §3.2 ⇆ tech §2).** Implementation seals segments on size-roll only; a complete record keeps its hot segment `.open` and is recognised by a terminal `substrate.RunFinalised` frame. Matches the design-spec example; tech §2 prose says "seal on finalise." Candidate clarification for a tech-spec revision (flow-back note); watch that no reader assumes a complete record has no `.open` segment.

- **TriggerFired carries `factory`+`instance` (strict validator-extras).** The runtime emits both on every TriggerFired but the locked v0.1 TriggerFired schema declares neither; lifecycle events bypass `_resolve`'s schema validation so nothing catches this at runtime. Filed `P-TRIGGERFIRED-FACTORY` (pre-existing) + `P-TRIGGERFIRED-INSTANCE` (Architect-directed, closes F-OBS-2/check 11). Carried implemented-ahead-of-ratification per the P-SUBJECT-ID precedent. Escalate at the v0.2 version bump / a conformance harness that validates lifecycle frames against the manifest schemas — until then these frames would fail a strict re-validation against the v0.1 schema.

- **Lifecycle payloads bypass the §4.2 whitelist.** `_resolve`'s `_Lifecycle` branch returns runtime-built payloads unchecked. Realistic exposure is small (`measured_us` in range, error strings fine), but a runtime-built payload with an out-of-range value would crash at frame time inside `_cycle` and land in the writer-fail path rather than being recorded. Watch when new lifecycle fields are added; consider routing lifecycle payloads through `try_canonical` too if any numeric/foreign field appears.

- **D-8 comparison normalizes away run-varying identity (run_id, instance ULIDs).** `inspect.first_divergence` / `_payload_hash` strip `run_id` and `instance` and reduce `producer` refs to `{kind}` before hashing, so two runs of the *same deterministic topology* are D-8-equivalent (else they'd always diverge at frame 0 on run_id, defeating conformance check 13 "localize the perturbed *decision*"). The spec's D-8 is "(event-kind sequence, decision-identity sequence, canonical payload hashes), supplementary metadata excluded" — it names t/host/config but does NOT enumerate run_id/instance. The exact exclusion set is an implementation refinement; flag as a tech-spec §14 flow-back (pin the D-8 normalization set: run_id, per-Producer instance/parent ULIDs, t). Watch that `input_sha256` and `firing_key` (the actual decision identities) are NOT stripped.

- **view_at takes a View instance, not a view name (tech §16 signature drift).** The spec §16 signature is `view_at(record, seq, view: str)` (by name, assuming the topology's View code is available). Implemented as `view_at(record, seq, view: View)` (an instance) because a record stores event payloads, not View code — the caller must supply the View whose update()/subscription define the fold. Tech-spec §14/§16 flow-back: show the instance form (or document that the name form requires a live topology registry). Already noted in inspect.py.

- **PredicateQuarantined emits union fields, not the discriminated shape.** `_quarantine` always sets `measured_us`+`k` even on the `reason:"exception"` path (locked schema marks them budget-only; `error` exception-only). Known soft spot — the vocab flags it as a PAYLOAD_FIELD_PROPOSED discriminated schema. Reconcile when that proposal is ratified.

---

## Sprint tail

*Agent maintains. Last 10 sprint summaries; older entries roll into `## Built` as compressed paragraphs.*

### Guided demo (2026-06-13, closed) — demo.sh + docs/demo.md
- **Scope:** a legible, runnable walk through the runtime from the committed reference records (R-1/R-2/R-3 + provenance + the conformance gate); `demo.sh` runnable end-to-end, `docs/demo.md` the annotated version, linked first under README → Docs. Architect-requested ("get them legible as a demo").
- **Dual contract:** signal — the three records' sequences (narrated in the Rubber Duck Pass, ## Surfaced for review); artifact — demo.sh exits 0 (no LLM / no network), docs/demo.md embeds verbatim output, all reproduced byte-identically from the committed records.
- **Rubber Duck Pass:** run at session close over the three records as the signal trace (## Surfaced for review) — no missing pairs / order violations / vocab gaps / payload anomalies / timing surprises; tone re-checked; the perf-portability note written into the demo as-is.
- **Closed:** clean.

### Re-root + private GitHub + real CI (2026-06-13, closed) — packaging + a CI backstop
- **Scope:** re-root the repo at `substrate/` (subtree split, history preserved), add the private `laffeyp/Substrate` origin on main + force-push (only the substrate/ tree tracked), author `.gitignore`, stand up GitHub Actions CI (ubuntu+macOS × py3.12/3.13/3.14: ruff + format + mypy --strict + pytest + lint-imports + conformance --no-perf). Architect-directed.
- **Dual contract:** artifact — CI green 6/6; repo private, default main; F-API-6 import-linter-in-CI claim now literally true.
- **Rubber Duck Pass (CI as the external check surface):** the first CI run caught a real defect — `httpx` imported by `reference/_models.py` was declared only in `[openai-compat]`, not `[dev]`; passed locally only because the dev venv carried it. Fixed (httpx → `[dev]`), re-locked, green. Action majors bumped (checkout v5, setup-uv v6) to clear the Node-20 deprecation; CI re-verified.
- **Closed:** clean.

### "Does it do what it says" audit close (2026-06-13, closed) — presentation-layer honesty
- **Scope:** close the external 24-agent audit (reviews #8–#13). Findings, all closed: R-3 fake checker → real `ast.parse`; uncommitted "recorded runs" → committed deterministic CI-mode records (replayable, hash-verified) + a CI-mode label README; "proves" → "checks/exercises"; dead `LET_FINISH` path removed; rich markup eating status text → `Console(markup=False)`; aspirational import-linter/CI claims → made true by the re-root/CI sprint.
- **Dual contract:** signal — reference records' sequences; artifact — 151 tests + 1 opt-in perf skip; `substrate conformance --no-perf` = 15 PASS / 0 FAIL / 1 DEFERRED / 1 SKIPPED; records replay + hash-verify with no Ollama.
- **Rubber Duck Pass:** verdict after close — ship. The kernel was the real thing; this arc made the packaging and presentation match it.
- **Closed:** clean. The one open item is the Architect's `v1.0.0` tag (parked).

### Wave 9 Batch A — conformance harness + perf probe (2026-06-13, closed) — the release gate
- **Scope:** the 17-check suite behind `substrate conformance` (check 6 a genuine DEFERRED third state; check 15 honest perf measurement) + handshake-#4 composition/api fixes (default_export failure-carrier, required inner_root, replay-exception re-exports). Auto-within-phase.
- **Dual contract:** signal — 17 canonical topologies run; artifact — 133 tests + 1 honest perf xfail, four gates green, mypy --strict (28 files).
- **Rubber Duck Pass:** between-batch independent review pending before commit.
- **HONEST BLOCKER SURFACED:** N-PERF-1 floor not met (~32K/sec ref shape vs 100K) — root-caused (per-event asyncio round-trip), batch-drain fix recommended, Architect decision needed (## Surfaced for review). NOT fudged.
- **Closed:** clean (pending review + perf ruling). Next batches: R-1/R-2/R-3 reference topologies (dual-mode), docs (N-DOC-1), resolve the b.export two-source + P-COMPOSITION-INNER-PROVENANCE before 1.0.

### Wave 8 — composition / export maps (2026-06-13, closed) — substrate as a Producer
- **Scope:** `composition.py` (embedded_substrate + EmbeddedRunFailed), manifest export-map + ProducerFailed inner_run_id wiring, closing conformance check 7. Plus handshake-#3 Fixes 1 (incremental attach read) + 2 (api exception re-exports / type-based CLI catch). Auto-within-phase.
- **Dual contract:** signal — composition boundary sequences over real records; artifact — 125 tests, four gates green, mypy --strict (26 files).
- **Rubber Duck Pass (independent review agent vs §20 / F-COMP / check 7):** 2 invariants verified sound (boundary correctness + single-path backpressure); 4 divergences fixed — #1 per-frame inner provenance surfaced as `P-COMPOSITION-INNER-PROVENANCE` (run-granularity provenance holds), #2 default-export implemented (`default_export`), #3 `b.export` wired to manifest, #4 robust inner_run_id from RunResult. Post-fix review pending before commit.
- **Closed:** clean (pending review). Next: Wave 9 (conformance harness + reference topologies + docs — carry-forwards (a) byte-identical-replay doc qualifier + (b) check-6 genuine three-state come due).

### Wave 7 — live attach + CLI + sidecars (2026-06-13, closed) — the reader surfaces
- **Scope:** `attach.py` (F-PERS-4 read-only follower), `sidecar.py` (writer-stats + diagnostics, off-bus), `cli.py` (8 commands, api-only), check 14 (diagnostic invariance) + external-review #2 Fixes A (v0.2 surgical-diff) and B (D-8 finalisation_payload_dropped). Auto-within-phase.
- **Dual contract:** signal — attach/tail/inspect/replay/stats sequences over real records; artifact — 112 tests, four gates green, mypy --strict (25 files).
- **Rubber Duck Pass (independent review agent, grounded vs tech §13/§6.4/§3.8/§17 + design §5 + F-PERS-4/F-OBS-6/F-CLI + check 14):**
  - *BLOCKER "segment-roll double-yield in the follower"* → **resolved-here**: the attach cursor was keyed by basename, which changes on seal (`events-N.open.jsonl`→`events-N.jsonl`), resetting the cursor to 0 and re-yielding the whole segment. Re-keyed on the roll-stable numeric segment INDEX; regression test forces rolls (shrunk SEGMENT_MAX_BYTES) and asserts no duplicate/skipped seq across the roll.
  - *MAJOR "--producer prefix matching unimplemented"* → resolved-here (kind-prefix match per design §5.2).
  - *MAJOR "run does not print root on config-error paths"* → resolved-here (root computed up front, printed on the topology-load / registration / lock paths; the missing-spec arg error has no root and stays bare).
  - *MAJOR "run --tail/--verbose missing"* → resolved-here (concurrent read-only follower task streams events to stderr during the run; --verbose includes substrate.* ; tests for both).
  - *MINOR "read_record follows symlinks (§17)"* → resolved-here (read_record now opens O_NOFOLLOW, matching the follower).
  - *MINOR "sidecar flush could raise in finally"* → resolved-here (flushes guarded; a sidecar OSError never turns a finalised bus into an API-boundary exception).
  - *MINOR "BusLockedError caught by class-name string"* → noted: works today (the lock raises in pre-asyncio setup, propagates unwrapped); left as-is, flagged for a stable `api` sentinel later.
  - *OK*: F-PERS-4 read-only verified at the syscall level; check-14 diagnostic invariance bit-identical by construction; sidecars off-bus/never-sequenced; F-API-6 AST import-lint complete; exit codes + 3b-deferred + conformance-not-false-green all correct.
- **Closed:** clean. Next: Wave 8 (composition / export maps).

### Vocabulary v0.2 + D-8 honesty (2026-06-13, closed) — first vocabulary evolution
- **Scope:** Architect Ruling 1 — ratify P-TRIGGERFIRED-INSTANCE/FACTORY/INPUT-BLOB into an additive `signals/0.2.json` (+ rationale); wire VOCAB_VERSION into the record; fold external-review #1 D-8 fixes (exclude measured_us / normalize error from the equivalence relation) + RecordWriter-error propagation.
- **Dual contract:** signal vacuous (content/vocab bump + one new manifest field); artifact — 90 tests, four gates green, signals JSON valid.
- **Rubber Duck Pass (own review, grounded vs product §D-8 + the three ratified proposals):** additive diff confirmed (metadata + TriggerFired tag only; all else byte-identical to v0.1); D-8 measured_us false-divergence closed with a two-run budget-quarantine regression test; error-repr normalized; carry-ahead proposals now `ratified:true`.
- **Closed:** clean. Next: the Level-3(b) spec amendment (Ruling 2), then Wave 7.

### Wave 6 — replay + inspection (2026-06-13, closed) — the read-side reconstruction surface
- **Scope:** `inspect.py` (explain_producer/trace_ancestry/view_at/decisions_between/first_divergence + Explanation/Divergence) and `replay.py` (Levels 1/2/3a + 3b deferred), closing conformance checks 11/12/13. Auto-within-phase.
- **Dual contract:** signal — provenance/view-at/divergence/replay sequences on records; artifact — 89 tests, all four gates green.
- **Rubber Duck Pass (independent review agent vs tech §12/§14 + product §7 checks 6/11/12/13 + D-8):**
  - *MAJOR "_d8_normalize strips run_id/instance globally by key name"* → **resolved-here**: scoped the strip to `substrate.*` lifecycle frames only; application payloads compared verbatim (an app field named `instance`/`run_id` is no longer silently erased — would have been a false-negative for check 13).
  - *MINOR "Level 2 silently skips a malformed firing"* → **resolved-here**: a TriggerFired with input_sha256 but neither input field (D-5 violated) is now flagged as a mismatch; the blob-unresolvable-from-bare-iterable case stays an honest skip.
  - *BLOCKER "instance/factory unratified under strict vocab"*, *BLOCKER "3(b) vs F-RPLY-1 MUST/check 6"*, *MINOR "view_at signature"* → **surfaced** (see ## Surfaced for review — three Architect rulings; carry-ahead proposals + the deferral are already filed/documented).
  - *OK*: provenance closure acyclic + rooted + ProducerNotFound on dangling; view_at boundary as-of-N correct; Level-2 blob re-hash byte-identical to input_sha256; 3(a) gate reads manifest determinism + ceiling, refuses honestly; 3(b) genuinely deferred not faked.
- **Closed:** clean (3 Architect rulings surfaced; none blocking further building). Next: Wave 7 (live attach + CLI + sidecars).

### Safety net + review-pass-2 correctness (2026-06-13, closed) — non-termination guard + 5 correctness fixes
- **Scope:** Priority A (pytest-timeout + liveness regression tests) + Priority B (PerKey/cooldown data loss, lock-outside-try leak, swallowed kernel errors, dead finalisation_payload, overloaded $blob sentinel). Driven by an Architect review pass 2 after a non-terminating revision overloaded the shared machine.
- **Dual contract:** signal — liveness/cooldown/finalisation/kernel-error sequences on the record; artifact — 87 tests, all four gates green.
- **Rubber Duck Pass:** pending the independent review agent (run before the commit, per protocol).
- **Closed:** clean (pending review). NEXT is the runtime.py God-module refactor (hard gate) before Wave 6 resumes. Wave 6 inspect/replay code is written + green in-tree but held uncommitted until after the refactor.

### Priority-1 robustness hardening (2026-06-13, closed) — external-review fixes against the live runtime
- **Scope:** the 6 external-review findings — try/finally run body, the shared sanitize-or-log helper across 5 ingestion points, blob offload wiring, four silent no-ops (persistent lock / logical cooldown / watchdog seconds / status=failed), ruff-gate reconciliation, empty-View guard. Auto-within-phase.
- **Dual contract:** signal — view-failure/cooldown/blob/lock sequences asserted on the record; artifact — 59 tests, ruff check + ruff format --check clean, mypy --strict clean (18 files).
- **Rubber Duck Pass (TWO independent review agents, grounded vs kernel v15 §6 / tech §3.7,§6.2,§6.3,§10 / vocab 0.1 / product §7):**
  - *BLOCKER "double-finalise after view failure"* → **CONFIRMED, resolved-here**: a mid-cascade view failure could append queued control events after the terminal RunFinalised. Added a `_terminated` guard at the top of `_cycle` + `_control.clear()` in `_view_failure`; test proves exactly one RunFinalised, last frame, dense seq, incl. a view raising on a control event (TriggerFired).
  - *BLOCKER "$blob nested inside resolved_input"* → **CONFIRMED, resolved-here**: oversized TriggerFired input was placed as the value of `resolved_input` instead of the top-level `$blob` field (locked schema requires exactly-one-of). Refactored to `_resolved_input_fields` returning the discriminated fragment; test asserts the mutual-exclusivity.
  - *MAJOR "cooldown counts raw seq not subscription-matched cycles"* (kernel §6) → resolved-here: now counts subscription-matching appends per trigger (`_trigger_match_count`).
  - *MAJOR "double cooldown for WhileTrue+Logical"* → resolved-here: `WhileTrue` no longer self-throttles; the builder lifts its cooldown to the trigger level; single enforcement point.
  - *MAJOR "hash from unsealed object"* → **investigated, original behavior is correct**: `msgspec.to_builtins` cannot encode the sealed `MappingProxyType` (verified); hashing the pre-seal value yields identical canonical bytes (JCS folds dict/MappingProxyType identically), so `input_sha256` is honest. Comment added.
  - *MINOR* (lifecycle payloads bypass the whitelist; PredicateQuarantined emits union fields) → deferred to Drift watchlist.
  - Second pass verified all five fixes HOLD, no new issues.
- **Vocabulary:** `factory`/`instance` on TriggerFired are uncatalogued under strict validator-extras → filed `P-TRIGGERFIRED-INSTANCE` (PAYLOAD_FIELD_PROPOSED, Architect-directed, closes F-OBS-2/check 11) and updated `P-TRIGGERFIRED-FACTORY` disposition; carried implemented-ahead-of-ratification per the P-SUBJECT-ID precedent. Surfaced, not silently invented.
- **Closed:** clean. Next: Wave 6 proper (replay Levels 1–3a + inspection/provenance/divergence — checks 6, 12, 13).

### Waves 3–5 (2026-06-13, closed) — writer + append cycle + primitives + RunStarted
- **Scope:** the runtime heart, end-to-end runnable against the design §3.2/§3.3 oracles. Auto-within-phase.
- **Dual contract:** signal — count_to_three emits the exact §3.2 sequence; artifact — 42 tests, ruff, mypy --strict clean.
- **Rubber Duck Pass (independent review agent; grounded vs kernel v15 / tech §6,§8 / vocab 0.1):**
  - *BLOCKER "clear `_staged` per cycle"* → **REJECTED (reviewer error)**: kernel Decision #8 mandates a staged message be visible to the staging cycle AND all later cycles (Routes feed future instantiations); clearing would break Retry. Added a guard comment + `test_routes` proving same-cycle visibility. (resolved-here)
  - *MAJOR "input sealing (§8.3) not enforced"* → **CONFIRMED, resolved-here**: added `sealing.seal` (recursive immutability-by-construction; dict→MappingProxyType, list→tuple; raw bytes/arbitrary → `InputTypeError` → `InputBuildFailed`). Reconciled design §3.3 dict-inputs vs §8.3 frozen-only by *converting* rather than rejecting. Tests added.
  - *MAJOR "quiescence missing true-and-unfired clause"* → **addressed**: added the control-queue-empty check; the true-and-unfired clause is vacuous under logical cooldowns (kernel §Quiescence — triggers fire only on appends; none coming ⇒ none mature); wall-clock-timer quiescence deferred.
  - *MINOR* → resolved-here: reset violation counter on quarantine; `firing_key="__initial__"` for initial Producers.
- **Closed:** clean. Next: Wave 6 (replay Levels 1–3b + inspection/provenance/divergence — checks 6, 12, 13).

### Wave 2 (2026-06-13, closed) — run record on disk
- **Scope:** framing (CRC), torn-tail recovery, segments + seal-on-roll, blob store, fsync/durability policies, persistent-bus locking (tech §3, §5, §11). Auto-within-phase.
- **Dual contract:** signal vacuous; artifact pass — 29 tests, ruff clean, mypy --strict clean.
- **Conformance cores:** check 16 (torn-tail recovery, exact truncation) + check 10 (lock contention) tested in miniature.
- **Rubber Duck Pass:** (payload anomaly) `os.F_FULLFSYNC` doesn't exist → silent fsync degrade → *resolved-here* (real `fcntl.F_FULLFSYNC`); (spec ambiguity) seal-on-finalise vs seal-on-roll → *resolved-here* via design §3.2 (seal-on-roll, completeness=terminal RunFinalised); no order/pairing violations (no runtime trace yet).
- **Closed:** clean. Next: Wave 3 (the writer + the 6-step append cycle + validation at the boundary — checks 1, 2, 3, 4).

### Wave 0 + S-1.1 (2026-06-13, closed) — skeleton, contracts, canonical encoding
- **Scope:** stand up the Python package (src layout, py3.12+ via uv), the shared contract types (envelope + Producer/View protocols + constants), and the Wave-1 canonical-encoding root. Auto-within-phase (Architect autonomy directive).
- **Dual contract:** signal vacuous (runtime not built); artifact pass — 15 tests, ruff clean, mypy --strict clean.
- **API fidelity:** msgspec 0.21.1 + rfc8785 0.1.4 surfaces VERIFIED by direct introspection of the installed libraries (frozen→AttributeError; ValidationError with path; to_builtins base64s raw bytes; dumps→bytes; CanonicalizationError) — bridge mapping moved STUB→VERIFIED. No invented APIs.
- **Deferred:** S-1.INT official RFC 8785 conformance vectors (must be FETCHED from the spec, not fabricated — see ## Deferred) + a hypothesis round-trip property test.
- **Closed:** clean. Next: Wave 2 (run record on disk — segments/frame-CRC/recovery/manifest/blob/durability).

### Sprint 000 (2026-06-12, closed) — Vocabulary Session
- **Scope:** lock `signals/0.1.json` + rationale via the 12-step BOOTSTRAP procedure, preceded by a research pass; draft-all-then-review cadence; strict validator-extras.
- **Dual contract:** signal vacuous (content sprint, no runtime emission); artifact pass (4 signals files + v16 note authored; JSON validates).
- **Method:** 15-agent parallel workflow (4 research → 6 subsystem drafters reading originals → 1 synthesizer → 4 adversarial verifiers); first run aborted, resumed from journal cache losslessly.
- **Rubber Duck Pass (narrated self-review; no runtime trace):** external check surfaces = the locked vocabulary, the 17 conformance checks, the canonical specs. Observations: (payload anomaly) synthesizer fabricated `vocab_version` → *resolved-here* (removed); (vocabulary gap) lifecycle/injection subject-identity not spec-named but required by checks 1/11/R-1 → *surfaced* → Architect ratified into v0.1 + flagged kernel v16; (payload anomaly) asserted-as-fact `ProducerEmittedInvalidEvent` producer claim → *resolved-here* (demoted to ratified proposal); (vocabulary gap) `TriggerFired.factory` kernel-vs-tech drift → *deferred* (P-TRIGGERFIRED-FACTORY / v16 R-4).
- **Outcome:** v0.1 LOCKED, signed. 3 proposals ratified into v0.1; kernel v16 reconciliation note filed. Closed clean (no unresolved halts).

---

## Single-writer-per-section discipline

| Section | Who writes | What they write |
|---|---|---|
| `## Surfaced for review` | Agent + Architect | Halts (Agent), comprehension affirmations (Agent), partial verdicts (Agent), proposed decisions (Agent), specific feedback (Architect) |
| `## Decisions` | Architect ONLY (append-only) | Project scope; binding decisions; resolutions of halts |
| `## Built` | Agent (append-only) | One paragraph per sprint close |
| `## Deferred` | Anyone | Items deferred with re-visit condition |
| `## Open questions` | Anyone | Questions not yet answered |
| `## Drift watchlist` | Agent maintains | Observations to track across sprints |
| `## Sprint tail` | Agent maintains | Last 10 sprint closes |

---

*BLACKBOARD.md for Substrate. First session 2026-06-12. One COMPREHENSION_AFFIRMATION on file. One proposed scope Decision awaiting Architect ratification. Sprint 0 (Vocabulary Session) is the founding act per hard rule 12; no implementation sprint dispatches until `signals/0.1.json` + `signals/0.1-rationale.md` are locked and signed.*
