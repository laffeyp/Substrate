Review the diff for the role assigned. Read the changed files first,
then the surrounding code the diff touches. Name the file and the
line for every claim.

State what the change does before you say whether it is right. Then
give one verdict — accept, block, or ask — with the reason in one
sentence. Do not repeat what other reviewers wrote; a shared concern
gets one line, not five.
